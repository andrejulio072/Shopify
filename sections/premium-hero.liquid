{{ 'premium-hero.css' | asset_url | stylesheet_tag }}

<style>
  .premium-hero {
    position: relative;
    min-height: 80vh;
    display: flex;
    align-items: center;
    background: #ffffff;
    overflow: hidden;
  }

  .premium-hero__container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 20px;
    width: 100%;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }

  .premium-hero__content {
    z-index: 2;
  }

  .premium-hero__badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: #f8f9fa;
    color: #2c3e50;
    padding: 8px 16px;
    border-radius: 25px;
    font-size: 14px;
    font-weight: 600;
    margin-bottom: 20px;
    border: 1px solid #e9ecef;
  }

  .premium-hero__badge-icon {
    font-size: 16px;
  }

  .premium-hero__title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    line-height: 1.1;
    color: #000000;
    margin-bottom: 20px;
    letter-spacing: -0.02em;
  }

  .premium-hero__title--highlight {
    background: linear-gradient(135deg, #000000 0%, #4a4a4a 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .premium-hero__subtitle {
    font-size: 18px;
    color: #6c757d;
    line-height: 1.6;
    margin-bottom: 30px;
    max-width: 480px;
  }

  .premium-hero__actions {
    display: flex;
    gap: 20px;
    margin-bottom: 40px;
    flex-wrap: wrap;
  }

  .premium-hero__btn {
    padding: 15px 35px;
    border-radius: 8px;
    font-weight: 600;
    font-size: 16px;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    transition: all 0.3s ease;
    border: 2px solid transparent;
    min-width: 180px;
    justify-content: center;
  }

  .premium-hero__btn--primary {
    background: #000000;
    color: #ffffff;
  }

  .premium-hero__btn--primary:hover {
    background: #2c3e50;
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
    color: #ffffff;
  }

  .premium-hero__btn--secondary {
    background: transparent;
    color: #000000;
    border-color: #000000;
  }

  .premium-hero__btn--secondary:hover {
    background: #000000;
    color: #ffffff;
  }

  .premium-hero__stats {
    display: flex;
    gap: 30px;
    flex-wrap: wrap;
  }

  .premium-hero__stat {
    text-align: left;
  }

  .premium-hero__stat-number {
    display: block;
    font-size: 24px;
    font-weight: 800;
    color: #000000;
    line-height: 1;
  }

  .premium-hero__stat-label {
    font-size: 14px;
    color: #6c757d;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-top: 4px;
  }

  .premium-hero__visual {
    position: relative;
    height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .premium-hero__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 12px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
  }

  .premium-hero__video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 12px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
  }

  .premium-hero__play-btn {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 80px;
    height: 80px;
    background: rgba(255, 255, 255, 0.9);
    border: none;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
  }

  .premium-hero__play-btn:hover {
    transform: translate(-50%, -50%) scale(1.1);
    background: rgba(255, 255, 255, 1);
  }

  .premium-hero__floating-card {
    position: absolute;
    background: #ffffff;
    padding: 20px;
    border-radius: 12px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    z-index: 2;
  }

  .premium-hero__floating-card--top {
    top: 20px;
    right: 20px;
    text-align: center;
  }

  .premium-hero__floating-card--bottom {
    bottom: 20px;
    left: 20px;
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .premium-hero__floating-avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    object-fit: cover;
  }

  .premium-hero__floating-text {
    font-size: 14px;
    color: #2c3e50;
    margin: 0;
  }

  .premium-hero__floating-stars {
    color: #ffc107;
    font-size: 12px;
    margin-top: 2px;
  }

  /* Responsive */
  @media screen and (max-width: 1200px) {
    .premium-hero__container {
      gap: 40px;
    }
  }

  @media screen and (max-width: 990px) {
    .premium-hero__container {
      grid-template-columns: 1fr;
      gap: 40px;
      text-align: center;
    }

    .premium-hero__visual {
      height: 400px;
      order: -1;
    }

    .premium-hero__stats {
      justify-content: center;
    }
  }

  @media screen and (max-width: 768px) {
    .premium-hero {
      min-height: 70vh;
      padding: 60px 0;
    }

    .premium-hero__container {
      padding: 0 15px;
      gap: 30px;
    }

    .premium-hero__visual {
      height: 300px;
    }

    .premium-hero__actions {
      flex-direction: column;
      align-items: center;
    }

    .premium-hero__btn {
      width: 100%;
      max-width: 280px;
    }

    .premium-hero__stats {
      gap: 20px;
      justify-content: center;
    }

    .premium-hero__floating-card {
      display: none;
    }
  }

  /* Animation */
  .premium-hero__content > * {
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.8s ease forwards;
  }

  .premium-hero__badge {
    animation-delay: 0.1s;
  }

  .premium-hero__title {
    animation-delay: 0.2s;
  }

  .premium-hero__subtitle {
    animation-delay: 0.3s;
  }

  .premium-hero__actions {
    animation-delay: 0.4s;
  }

  .premium-hero__stats {
    animation-delay: 0.5s;
  }

  .premium-hero__visual {
    opacity: 0;
    transform: scale(0.9);
    animation: scaleIn 1s ease forwards 0.3s;
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes scaleIn {
    to {
      opacity: 1;
      transform: scale(1);
    }
  }
</style>

<section class="premium-hero">
  <div class="premium-hero__container">
    
    <!-- Content -->
    <div class="premium-hero__content">
      
      {%- unless section.settings.badge_text == blank -%}
        <div class="premium-hero__badge">
          <span class="premium-hero__badge-icon">{{ section.settings.badge_icon | default: "🔥" }}</span>
          <span>{{ section.settings.badge_text }}</span>
        </div>
      {%- endunless -%}

      <h1 class="premium-hero__title">
        {%- unless section.settings.title_part1 == blank -%}
          {{ section.settings.title_part1 }}<br>
        {%- endunless -%}
        {%- unless section.settings.title_part2 == blank -%}
          <span class="premium-hero__title--highlight">{{ section.settings.title_part2 }}</span>
        {%- endunless -%}
      </h1>

      {%- unless section.settings.subtitle == blank -%}
        <p class="premium-hero__subtitle">{{ section.settings.subtitle }}</p>
      {%- endunless -%}

      <div class="premium-hero__actions">
        {%- unless section.settings.btn_primary_text == blank -%}
          <a href="{{ section.settings.btn_primary_link }}" class="premium-hero__btn premium-hero__btn--primary">
            <span>{{ section.settings.btn_primary_text }}</span>
            <span>→</span>
          </a>
        {%- endunless -%}

        {%- unless section.settings.btn_secondary_text == blank -%}
          <a href="{{ section.settings.btn_secondary_link }}" class="premium-hero__btn premium-hero__btn--secondary">
            {{ section.settings.btn_secondary_text }}
          </a>
        {%- endunless -%}
      </div>

      {%- if section.settings.show_stats -%}
        <div class="premium-hero__stats">
          <div class="premium-hero__stat">
            <span class="premium-hero__stat-number">{{ section.settings.stat1_number | default: "50k+" }}</span>
            <span class="premium-hero__stat-label">{{ section.settings.stat1_label | default: "Clientes" }}</span>
          </div>
          <div class="premium-hero__stat">
            <span class="premium-hero__stat-number">{{ section.settings.stat2_number | default: "4.9★" }}</span>
            <span class="premium-hero__stat-label">{{ section.settings.stat2_label | default: "Avaliação" }}</span>
          </div>
          <div class="premium-hero__stat">
            <span class="premium-hero__stat-number">{{ section.settings.stat3_number | default: "98%" }}</span>
            <span class="premium-hero__stat-label">{{ section.settings.stat3_label | default: "Satisfação" }}</span>
          </div>
        </div>
      {%- endif -%}
    </div>

    <!-- Visual -->
    <div class="premium-hero__visual">
      {%- if section.settings.media_type == 'video' and section.settings.video_url != blank -%}
        <video class="premium-hero__video" autoplay muted loop playsinline>
          <source src="{{ section.settings.video_url }}" type="video/mp4">
        </video>
        {%- if section.settings.show_play_button -%}
          <button class="premium-hero__play-btn" onclick="toggleVideo(this)">
            ▶️
          </button>
        {%- endif -%}
      {%- elsif section.settings.image != blank -%}
        {{
          section.settings.image
          | image_url: width: 800
          | image_tag:
            loading: 'eager',
            class: 'premium-hero__image',
            alt: section.settings.image.alt | escape
        }}
      {%- else -%}
        <div style="width: 100%; height: 100%; background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%); border-radius: 12px; display: flex; align-items: center; justify-content: center; color: #6c757d; font-size: 18px;">
          Adicione uma imagem ou vídeo
        </div>
      {%- endif -%}

      {%- if section.settings.show_floating_cards -%}
        <!-- Floating Cards -->
        <div class="premium-hero__floating-card premium-hero__floating-card--top">
          <p class="premium-hero__floating-text" style="margin: 0; font-weight: 600;">{{ section.settings.floating_card1_text | default: "Frete Grátis" }}</p>
          <p class="premium-hero__floating-text" style="margin: 0; font-size: 12px;">{{ section.settings.floating_card1_subtext | default: "Acima de R$ 99" }}</p>
        </div>

        <div class="premium-hero__floating-card premium-hero__floating-card--bottom">
          {%- if section.settings.floating_avatar != blank -%}
            {{
              section.settings.floating_avatar
              | image_url: width: 80
              | image_tag:
                class: 'premium-hero__floating-avatar',
                alt: 'Cliente satisfeito'
            }}
          {%- else -%}
            <div style="width: 40px; height: 40px; background: #e9ecef; border-radius: 50%; display: flex; align-items: center; justify-content: center;">👤</div>
          {%- endif -%}
          <div>
            <p class="premium-hero__floating-text">{{ section.settings.floating_card2_text | default: "João transformou seu corpo" }}</p>
            <div class="premium-hero__floating-stars">⭐⭐⭐⭐⭐</div>
          </div>
        </div>
      {%- endif -%}
    </div>
  </div>
</section>

<script>
function toggleVideo(button) {
  const video = button.parentElement.querySelector('video');
  if (video.paused) {
    video.play();
    button.style.opacity = '0';
  } else {
    video.pause();
    button.style.opacity = '1';
  }
}
</script>

{% schema %}
{
  "name": "Hero Premium",
  "settings": [
    {
      "type": "text",
      "id": "badge_text",
      "default": "Novo lançamento",
      "label": "Texto do badge"
    },
    {
      "type": "text",
      "id": "badge_icon",
      "default": "🔥",
      "label": "Ícone do badge"
    },
    {
      "type": "text",
      "id": "title_part1",
      "default": "Transforme Seu",
      "label": "Primeira parte do título"
    },
    {
      "type": "text",
      "id": "title_part2",
      "default": "Corpo",
      "label": "Segunda parte do título (destacada)"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "default": "Suplementos premium para resultados reais. Qualidade internacional, preços brasileiros.",
      "label": "Subtítulo"
    },
    {
      "type": "text",
      "id": "btn_primary_text",
      "default": "Começar Agora",
      "label": "Texto botão primário"
    },
    {
      "type": "url",
      "id": "btn_primary_link",
      "label": "Link botão primário"
    },
    {
      "type": "text",
      "id": "btn_secondary_text",
      "default": "Ver Produtos",
      "label": "Texto botão secundário"
    },
    {
      "type": "url",
      "id": "btn_secondary_link",
      "label": "Link botão secundário"
    },
    {
      "type": "header",
      "content": "Mídia"
    },
    {
      "type": "select",
      "id": "media_type",
      "options": [
        {
          "value": "image",
          "label": "Imagem"
        },
        {
          "value": "video",
          "label": "Vídeo"
        }
      ],
      "default": "image",
      "label": "Tipo de mídia"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Imagem"
    },
    {
      "type": "text",
      "id": "video_url",
      "label": "URL do vídeo (MP4)"
    },
    {
      "type": "checkbox",
      "id": "show_play_button",
      "default": true,
      "label": "Mostrar botão play (apenas para vídeo)"
    },
    {
      "type": "header",
      "content": "Estatísticas"
    },
    {
      "type": "checkbox",
      "id": "show_stats",
      "default": true,
      "label": "Mostrar estatísticas"
    },
    {
      "type": "text",
      "id": "stat1_number",
      "default": "50k+",
      "label": "Estatística 1 - Número"
    },
    {
      "type": "text",
      "id": "stat1_label",
      "default": "Clientes",
      "label": "Estatística 1 - Label"
    },
    {
      "type": "text",
      "id": "stat2_number",
      "default": "4.9★",
      "label": "Estatística 2 - Número"
    },
    {
      "type": "text",
      "id": "stat2_label",
      "default": "Avaliação",
      "label": "Estatística 2 - Label"
    },
    {
      "type": "text",
      "id": "stat3_number",
      "default": "98%",
      "label": "Estatística 3 - Número"
    },
    {
      "type": "text",
      "id": "stat3_label",
      "default": "Satisfação",
      "label": "Estatística 3 - Label"
    },
    {
      "type": "header",
      "content": "Cards flutuantes"
    },
    {
      "type": "checkbox",
      "id": "show_floating_cards",
      "default": true,
      "label": "Mostrar cards flutuantes"
    },
    {
      "type": "text",
      "id": "floating_card1_text",
      "default": "Frete Grátis",
      "label": "Card 1 - Texto"
    },
    {
      "type": "text",
      "id": "floating_card1_subtext",
      "default": "Acima de R$ 99",
      "label": "Card 1 - Subtexto"
    },
    {
      "type": "image_picker",
      "id": "floating_avatar",
      "label": "Avatar cliente (Card 2)"
    },
    {
      "type": "text",
      "id": "floating_card2_text",
      "default": "João transformou seu corpo",
      "label": "Card 2 - Texto"
    }
  ],
  "presets": [
    {
      "name": "Hero Premium"
    }
  ]
}
{% endschema %}