<!--<!--

================================================================================

🏆 GARCIA BUILDER - CATEGORIES V3.0🏆 PREMIUM MASTER CATEGORIES V3.0

Clean Product Categories GridWorld-Class Category Showcase

========================================Inspired by: Nike, Gymshark, Lululemon

-->Ultra-Premium Grid Layout | Interactive Hover

========================================

<style>-->

  .gb-categories {

    padding: 100px 0;<link rel="stylesheet" href="{{ 'premium-v2.css' | asset_url }}">

    background: #ffffff;

  }<style>

  /* ========================================

  .gb-categories-container {     🎨 PREMIUM CATEGORIES VARIABLES

    max-width: 1400px;  ======================================== */

    margin: 0 auto;  :root {

    padding: 0 40px;    --categories-spacing: 120px;

  }    --category-radius: 24px;

    --category-overlay: linear-gradient(180deg, transparent 0%, rgba(0, 0, 0, 0.8) 100%);

  .gb-categories-header {  }

    text-align: center;

    margin-bottom: 60px;  /* ========================================

  }     💎 MAIN SECTION

  ======================================== */

  .gb-categories-title {  .premium-master-categories {

    font-size: clamp(36px, 5vw, 48px);    padding: var(--categories-spacing) 40px;

    font-weight: 900;    background: linear-gradient(180deg, #000000 0%, #0a0a0a 100%);

    color: #1a1a1a;    position: relative;

    margin: 0 0 16px 0;    overflow: hidden;

    letter-spacing: -0.02em;  }

  }

  /* Decorative Background */

  .gb-categories-subtitle {  .categories-bg-pattern {

    font-size: 18px;    position: absolute;

    color: #6b6b6b;    inset: 0;

    margin: 0;    opacity: 0.03;

  }    background-image:

      radial-gradient(circle at 30% 40%, rgba(0, 122, 255, 0.15) 0%, transparent 60%),

  .gb-categories-grid {      radial-gradient(circle at 70% 60%, rgba(48, 209, 88, 0.15) 0%, transparent 60%);

    display: grid;    pointer-events: none;

    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));  }

    gap: 32px;

  }  .categories-container {

    max-width: 1600px;

  .gb-category-card {    margin: 0 auto;

    position: relative;    position: relative;

    height: 400px;    z-index: 1;

    border-radius: 8px;  }

    overflow: hidden;

    text-decoration: none;  /* ========================================

    transition: transform 0.3s ease;     📝 SECTION HEADER

  }  ======================================== */

  .categories-header {

  .gb-category-card:hover {    text-align: center;

    transform: translateY(-8px);    margin-bottom: 80px;

  }    animation: fade-slide-up 1s ease-out;

  }

  .gb-category-image {

    width: 100%;  .categories-badge {

    height: 100%;    display: inline-block;

    object-fit: cover;    padding: 10px 24px;

    transition: transform 0.6s ease;    background: linear-gradient(135deg, rgba(0, 122, 255, 0.2) 0%, rgba(48, 209, 88, 0.2) 100%);

  }    border: 1px solid rgba(0, 122, 255, 0.3);

    border-radius: 50px;

  .gb-category-card:hover .gb-category-image {    color: #007AFF;

    transform: scale(1.05);    font-size: 13px;

  }    font-weight: 700;

    text-transform: uppercase;

  .gb-category-overlay {    letter-spacing: 0.15em;

    position: absolute;    margin-bottom: 24px;

    inset: 0;  }

    background: linear-gradient(180deg, transparent 0%, rgba(26, 26, 26, 0.8) 100%);

    display: flex;  .categories-heading {

    align-items: flex-end;    font-size: clamp(42px, 6vw, 72px);

    padding: 32px;    font-weight: 900;

  }    color: white;

    margin-bottom: 20px;

  .gb-category-content h3 {    text-transform: uppercase;

    font-size: 24px;    letter-spacing: -0.02em;

    font-weight: 700;    line-height: 1.1;

    color: #ffffff;  }

    margin: 0 0 8px 0;

  }  .categories-heading-highlight {

    background: linear-gradient(135deg, #007AFF 0%, #30D158 100%);

  .gb-category-content p {    -webkit-background-clip: text;

    font-size: 14px;    -webkit-text-fill-color: transparent;

    color: rgba(255, 255, 255, 0.8);    background-clip: text;

    margin: 0;  }

  }

  .categories-description {

  @media (max-width: 768px) {    font-size: 18px;

    .gb-categories {    font-weight: 500;

      padding: 60px 0;    color: rgba(255, 255, 255, 0.6);

    }    max-width: 700px;

    margin: 0 auto;

    .gb-categories-container {    line-height: 1.7;

      padding: 0 24px;  }

    }

  /* ========================================

    .gb-categories-header {     🎯 CATEGORIES GRID

      margin-bottom: 40px;  ======================================== */

    }  .categories-grid {

    display: grid;

    .gb-categories-grid {    grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));

      grid-template-columns: 1fr;    gap: 30px;

      gap: 24px;    margin-bottom: 60px;

    }  }



    .gb-category-card {  /* Category Card */

      height: 320px;  .category-card {

    }    position: relative;

  }    height: 500px;

</style>    border-radius: var(--category-radius);

    overflow: hidden;

<section class="gb-categories">    cursor: pointer;

  <div class="gb-categories-container">    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);

    <div class="gb-categories-header">    animation: fade-slide-up 1s ease-out backwards;

      <h2 class="gb-categories-title">{{ section.settings.title | default: 'Categorias' }}</h2>  }

      <p class="gb-categories-subtitle">{{ section.settings.subtitle | default: 'Encontre os melhores suplementos para seu objetivo' }}</p>

    </div>  .category-card:nth-child(1) { animation-delay: 0.1s; }

  .category-card:nth-child(2) { animation-delay: 0.2s; }

    <div class="gb-categories-grid">  .category-card:nth-child(3) { animation-delay: 0.3s; }

      {% for block in section.blocks %}  .category-card:nth-child(4) { animation-delay: 0.4s; }

        <a href="{{ block.settings.link }}" class="gb-category-card">  .category-card:nth-child(5) { animation-delay: 0.5s; }

          {% if block.settings.image != blank %}  .category-card:nth-child(6) { animation-delay: 0.6s; }

            <img src="{{ block.settings.image | image_url: width: 600 }}" alt="{{ block.settings.title }}" class="gb-category-image">

          {% else %}  .category-card:hover {

            <img src="https://via.placeholder.com/600x800/1a1a1a/d4af37?text={{ block.settings.title }}" alt="{{ block.settings.title }}" class="gb-category-image">    transform: translateY(-15px) scale(1.02);

          {% endif %}    box-shadow: 0 30px 80px rgba(0, 122, 255, 0.4);

            }

          <div class="gb-category-overlay">

            <div class="gb-category-content">  /* Featured Card (First Item - Larger) */

              <h3>{{ block.settings.title }}</h3>  .category-card.featured {

              <p>{{ block.settings.description }}</p>    grid-column: span 2;

            </div>    height: 600px;

          </div>  }

        </a>

      {% endfor %}  /* Background Image */

    </div>  .category-image {

  </div>    width: 100%;

</section>    height: 100%;

    object-fit: cover;

{% schema %}    transition: transform 0.7s cubic-bezier(0.4, 0, 0.2, 1);

{  }

  "name": "Garcia Builder Categories",

  "tag": "section",  .category-card:hover .category-image {

  "class": "garcia-builder-categories",    transform: scale(1.1);

  "settings": [  }

    {

      "type": "text",  /* Gradient Overlay */

      "id": "title",  .category-overlay {

      "label": "Título",    position: absolute;

      "default": "Categorias"    inset: 0;

    },    background: var(--category-overlay);

    {    transition: opacity 0.5s ease;

      "type": "text",  }

      "id": "subtitle",

      "label": "Subtítulo",  .category-card:hover .category-overlay {

      "default": "Encontre os melhores suplementos para seu objetivo"    background: linear-gradient(180deg,

    }      transparent 0%,

  ],      rgba(0, 122, 255, 0.3) 50%,

  "blocks": [      rgba(0, 0, 0, 0.9) 100%

    {    );

      "type": "category",  }

      "name": "Categoria",

      "settings": [  /* Content */

        {  .category-content {

          "type": "image_picker",    position: absolute;

          "id": "image",    bottom: 0;

          "label": "Imagem"    left: 0;

        },    right: 0;

        {    padding: 40px;

          "type": "text",    z-index: 2;

          "id": "title",    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);

          "label": "Título",  }

          "default": "Proteínas"

        },  .category-card:hover .category-content {

        {    padding-bottom: 50px;

          "type": "text",  }

          "id": "description",

          "label": "Descrição",  /* Category Icon */

          "default": "Whey, Caseína e mais"  .category-icon {

        },    width: 70px;

        {    height: 70px;

          "type": "url",    background: linear-gradient(135deg, rgba(0, 122, 255, 0.3) 0%, rgba(48, 209, 88, 0.3) 100%);

          "id": "link",    backdrop-filter: blur(20px);

          "label": "Link"    border: 2px solid rgba(255, 255, 255, 0.2);

        }    border-radius: 18px;

      ]    display: flex;

    }    align-items: center;

  ],    justify-content: center;

  "presets": [    font-size: 32px;

    {    margin-bottom: 24px;

      "name": "Garcia Builder Categories",    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);

      "blocks": [    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);

        {  }

          "type": "category",

          "settings": {  .category-card:hover .category-icon {

            "title": "Proteínas",    transform: scale(1.1) rotate(5deg);

            "description": "Whey, Caseína, Isolado"    background: linear-gradient(135deg, rgba(0, 122, 255, 0.5) 0%, rgba(48, 209, 88, 0.5) 100%);

          }    box-shadow: 0 12px 40px rgba(0, 122, 255, 0.5);

        },  }

        {

          "type": "category",  /* Category Name */

          "settings": {  .category-name {

            "title": "Pré-Treino",    font-size: 32px;

            "description": "Energia e Foco"    font-weight: 900;

          }    color: white;

        },    margin-bottom: 12px;

        {    text-transform: uppercase;

          "type": "category",    letter-spacing: -0.02em;

          "settings": {    text-shadow: 0 4px 20px rgba(0, 0, 0, 0.8);

            "title": "Creatina",    transition: all 0.3s ease;

            "description": "Força e Performance"  }

          }

        }  .category-card:hover .category-name {

      ]    transform: translateX(10px);

    }    color: #007AFF;

  ]  }

}

{% endschema %}  /* Category Description */

  .category-description {
    font-size: 15px;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 20px;
    line-height: 1.6;
    max-height: 0;
    overflow: hidden;
    opacity: 0;
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .category-card:hover .category-description {
    max-height: 100px;
    opacity: 1;
    margin-bottom: 24px;
  }

  /* Product Count */
  .category-count {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 18px;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 10px;
    color: rgba(255, 255, 255, 0.8);
    font-size: 13px;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .category-card:hover .category-count {
    background: rgba(0, 122, 255, 0.3);
    border-color: rgba(0, 122, 255, 0.5);
    color: white;
  }

  /* CTA Arrow */
  .category-arrow {
    position: absolute;
    bottom: 40px;
    right: 40px;
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #007AFF 0%, #30D158 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    color: white;
    opacity: 0;
    transform: translateX(-20px);
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 8px 30px rgba(0, 122, 255, 0.5);
  }

  .category-card:hover .category-arrow {
    opacity: 1;
    transform: translateX(0);
  }

  /* ========================================
     🚀 VIEW ALL BUTTON
  ======================================== */
  .categories-footer {
    text-align: center;
    margin-top: 60px;
    animation: fade-slide-up 1s ease-out 0.8s backwards;
  }

  .view-all-button {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    padding: 20px 50px;
    background: linear-gradient(135deg, rgba(0, 122, 255, 0.2) 0%, rgba(48, 209, 88, 0.2) 100%);
    border: 2px solid rgba(0, 122, 255, 0.5);
    border-radius: 14px;
    color: white;
    font-size: 16px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    cursor: pointer;
    text-decoration: none;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .view-all-button::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, #007AFF 0%, #30D158 100%);
    opacity: 0;
    transition: opacity 0.4s ease;
  }

  .view-all-button:hover::before {
    opacity: 1;
  }

  .view-all-button:hover {
    transform: translateY(-4px) scale(1.05);
    border-color: transparent;
    box-shadow: 0 15px 50px rgba(0, 122, 255, 0.6);
  }

  .view-all-button span {
    position: relative;
    z-index: 1;
  }

  .view-all-arrow {
    font-size: 20px;
    transition: transform 0.4s ease;
    position: relative;
    z-index: 1;
  }

  .view-all-button:hover .view-all-arrow {
    transform: translateX(8px);
  }

  /* ========================================
     📱 RESPONSIVE DESIGN
  ======================================== */
  @media (max-width: 1200px) {
    .categories-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .category-card.featured {
      grid-column: span 2;
      height: 500px;
    }
  }

  @media (max-width: 768px) {
    .premium-master-categories {
      padding: 80px 20px;
    }

    .categories-header {
      margin-bottom: 50px;
    }

    .categories-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .category-card,
    .category-card.featured {
      height: 400px;
      grid-column: span 1;
    }

    .category-content {
      padding: 30px;
    }

    .category-icon {
      width: 55px;
      height: 55px;
      font-size: 26px;
    }

    .category-name {
      font-size: 26px;
    }

    .category-description {
      display: none;
    }

    .category-arrow {
      bottom: 30px;
      right: 30px;
      width: 50px;
      height: 50px;
      font-size: 20px;
    }

    .view-all-button {
      width: 100%;
      max-width: 100%;
      justify-content: center;
    }
  }

  /* ========================================
     ✨ ANIMATIONS
  ======================================== */
  @keyframes fade-slide-up {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

<!-- ========================================
     🏆 PREMIUM CATEGORIES HTML
======================================== -->

<section class="premium-master-categories">
  <div class="categories-bg-pattern"></div>

  <div class="categories-container">

    <!-- Section Header -->
    <div class="categories-header">
      {% if section.settings.badge_text != blank %}
        <span class="categories-badge">{{ section.settings.badge_text }}</span>
      {% endif %}

      <h2 class="categories-heading">
        {{ section.settings.heading | default: 'SHOP BY' }}
        <span class="categories-heading-highlight">{{ section.settings.heading_highlight | default: 'CATEGORY' }}</span>
      </h2>

      {% if section.settings.description != blank %}
        <p class="categories-description">{{ section.settings.description }}</p>
      {% endif %}
    </div>

    <!-- Categories Grid -->
    <div class="categories-grid">
      {% for block in section.blocks %}
        <a
          href="{{ block.settings.collection.url | default: '/collections/all' }}"
          class="category-card {% if forloop.first and section.settings.featured_first %}featured{% endif %}"
          {{ block.shopify_attributes }}
        >
          <!-- Background Image -->
          {% if block.settings.image != blank %}
            <img
              src="{{ block.settings.image | image_url: width: 800 }}"
              alt="{{ block.settings.title }}"
              width="800"
              height="600"
              loading="lazy"
              class="category-image"
            >
          {% else %}
            <div style="
              width: 100%;
              height: 100%;
              background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
            "></div>
          {% endif %}

          <!-- Overlay -->
          <div class="category-overlay"></div>

          <!-- Content -->
          <div class="category-content">
            {% if block.settings.icon != blank %}
              <div class="category-icon">{{ block.settings.icon }}</div>
            {% endif %}

            <h3 class="category-name">{{ block.settings.title | default: 'Category Name' }}</h3>

            {% if block.settings.description != blank %}
              <p class="category-description">{{ block.settings.description }}</p>
            {% endif %}

            {% if block.settings.collection != blank %}
              <span class="category-count">
                {{ block.settings.collection.products_count }} Products
              </span>
            {% endif %}
          </div>

          <!-- CTA Arrow -->
          <div class="category-arrow">→</div>
        </a>
      {% endfor %}
    </div>

    <!-- View All Button -->
    {% if section.settings.show_view_all %}
      <div class="categories-footer">
        <a href="{{ section.settings.view_all_link | default: '/collections' }}" class="view-all-button">
          <span>{{ section.settings.view_all_text | default: 'View All Categories' }}</span>
          <span class="view-all-arrow">→</span>
        </a>
      </div>
    {% endif %}

  </div>
</section>

{% schema %}
{
  "name": "Premium Master Categories",
  "tag": "section",
  "class": "premium-master-categories-section",
  "settings": [
    {
      "type": "header",
      "content": "Section Header"
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "Explore"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "SHOP BY"
    },
    {
      "type": "text",
      "id": "heading_highlight",
      "label": "Heading Highlight",
      "default": "CATEGORY"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Discover our premium collections designed for peak performance"
    },
    {
      "type": "header",
      "content": "Layout Options"
    },
    {
      "type": "checkbox",
      "id": "featured_first",
      "label": "Make first item featured (larger)",
      "default": true
    },
    {
      "type": "header",
      "content": "View All Button"
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "label": "Show View All Button",
      "default": true
    },
    {
      "type": "text",
      "id": "view_all_text",
      "label": "Button Text",
      "default": "View All Categories"
    },
    {
      "type": "url",
      "id": "view_all_link",
      "label": "Button Link"
    }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Category Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Category Image"
        },
        {
          "type": "text",
          "id": "icon",
          "label": "Icon (Emoji)",
          "default": "💪"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Category Title",
          "default": "Category Name"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Category Description",
          "default": "Premium products for your fitness journey"
        },
        {
          "type": "collection",
          "id": "collection",
          "label": "Link to Collection"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Premium Master Categories",
      "blocks": [
        {
          "type": "category",
          "settings": {
            "icon": "💪",
            "title": "Strength Training",
            "description": "Build muscle and increase power"
          }
        },
        {
          "type": "category",
          "settings": {
            "icon": "🏃",
            "title": "Cardio & Running",
            "description": "Boost endurance and stamina"
          }
        },
        {
          "type": "category",
          "settings": {
            "icon": "🧘",
            "title": "Yoga & Flexibility",
            "description": "Improve mobility and balance"
          }
        },
        {
          "type": "category",
          "settings": {
            "icon": "👕",
            "title": "Apparel",
            "description": "Premium athletic wear"
          }
        }
      ]
    }
  ]
}
{% endschema %}
