<!--<!--<!--

========================================

🏆 GARCIA BUILDER - CATEGORIES V3.0================================================================================

Clean Product Categories Grid

========================================🏆 GARCIA BUILDER - CATEGORIES V3.0🏆 PREMIUM MASTER CATEGORIES V3.0

-->

Clean Product Categories GridWorld-Class Category Showcase

<style>

  .gb-categories {========================================Inspired by: Nike, Gymshark, Lululemon

    padding: 100px 0;

    background: #ffffff;-->Ultra-Premium Grid Layout | Interactive Hover

  }

========================================

  .gb-categories-container {

    max-width: 1400px;<style>-->

    margin: 0 auto;

    padding: 0 40px;  .gb-categories {

  }

    padding: 100px 0;<link rel="stylesheet" href="{{ 'premium-v2.css' | asset_url }}">

  .gb-categories-header {

    text-align: center;    background: #ffffff;

    margin-bottom: 60px;

  }  }<style>



  .gb-categories-title {  /* ========================================

    font-size: clamp(36px, 5vw, 48px);

    font-weight: 900;  .gb-categories-container {     🎨 PREMIUM CATEGORIES VARIABLES

    color: #1a1a1a;

    margin: 0 0 16px 0;    max-width: 1400px;  ======================================== */

    letter-spacing: -0.02em;

  }    margin: 0 auto;  :root {



  .gb-categories-subtitle {    padding: 0 40px;    --categories-spacing: 120px;

    font-size: 18px;

    color: #6b6b6b;  }    --category-radius: 24px;

    margin: 0;

  }    --category-overlay: linear-gradient(180deg, transparent 0%, rgba(0, 0, 0, 0.8) 100%);



  .gb-categories-grid {  .gb-categories-header {  }

    display: grid;

    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));    text-align: center;

    gap: 32px;

  }    margin-bottom: 60px;  /* ========================================



  .gb-category-card {  }     💎 MAIN SECTION

    position: relative;

    height: 400px;  ======================================== */

    border-radius: 8px;

    overflow: hidden;  .gb-categories-title {  .premium-master-categories {

    text-decoration: none;

    transition: transform 0.3s ease;    font-size: clamp(36px, 5vw, 48px);    padding: var(--categories-spacing) 40px;

  }

    font-weight: 900;    background: linear-gradient(180deg, #000000 0%, #0a0a0a 100%);

  .gb-category-card:hover {

    transform: translateY(-8px);    color: #1a1a1a;    position: relative;

  }

    margin: 0 0 16px 0;    overflow: hidden;

  .gb-category-image {

    width: 100%;    letter-spacing: -0.02em;  }

    height: 100%;

    object-fit: cover;  }

    transition: transform 0.6s ease;

  }  /* Decorative Background */



  .gb-category-card:hover .gb-category-image {  .gb-categories-subtitle {  .categories-bg-pattern {

    transform: scale(1.05);

  }    font-size: 18px;    position: absolute;



  .gb-category-overlay {    color: #6b6b6b;    inset: 0;

    position: absolute;

    inset: 0;    margin: 0;    opacity: 0.03;

    background: linear-gradient(180deg, transparent 0%, rgba(26, 26, 26, 0.8) 100%);

    display: flex;  }    background-image:

    align-items: flex-end;

    padding: 32px;      radial-gradient(circle at 30% 40%, rgba(0, 122, 255, 0.15) 0%, transparent 60%),

  }

  .gb-categories-grid {      radial-gradient(circle at 70% 60%, rgba(48, 209, 88, 0.15) 0%, transparent 60%);

  .gb-category-content h3 {

    font-size: 24px;    display: grid;    pointer-events: none;

    font-weight: 700;

    color: #ffffff;    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));  }

    margin: 0 0 8px 0;

  }    gap: 32px;



  .gb-category-content p {  }  .categories-container {

    font-size: 14px;

    color: rgba(255, 255, 255, 0.8);    max-width: 1600px;

    margin: 0;

  }  .gb-category-card {    margin: 0 auto;



  @media (max-width: 768px) {    position: relative;    position: relative;

    .gb-categories {

      padding: 60px 0;    height: 400px;    z-index: 1;

    }

    border-radius: 8px;  }

    .gb-categories-container {

      padding: 0 24px;    overflow: hidden;

    }

    text-decoration: none;  /* ========================================

    .gb-categories-header {

      margin-bottom: 40px;    transition: transform 0.3s ease;     📝 SECTION HEADER

    }

  }  ======================================== */

    .gb-categories-grid {

      grid-template-columns: 1fr;  .categories-header {

      gap: 24px;

    }  .gb-category-card:hover {    text-align: center;



    .gb-category-card {    transform: translateY(-8px);    margin-bottom: 80px;

      height: 320px;

    }  }    animation: fade-slide-up 1s ease-out;

  }

</style>  }



<section class="gb-categories">  .gb-category-image {

  <div class="gb-categories-container">

    <div class="gb-categories-header">    width: 100%;  .categories-badge {

      <h2 class="gb-categories-title">{{ section.settings.title | default: 'Categorias' }}</h2>

      <p class="gb-categories-subtitle">{{ section.settings.subtitle | default: 'Encontre os melhores suplementos para seu objetivo' }}</p>    height: 100%;    display: inline-block;

    </div>

    object-fit: cover;    padding: 10px 24px;

    <div class="gb-categories-grid">

      {% for block in section.blocks %}    transition: transform 0.6s ease;    background: linear-gradient(135deg, rgba(0, 122, 255, 0.2) 0%, rgba(48, 209, 88, 0.2) 100%);

        <a href="{{ block.settings.link }}" class="gb-category-card">

          {% if block.settings.image != blank %}  }    border: 1px solid rgba(0, 122, 255, 0.3);

            <img

              src="{{ block.settings.image | image_url: width: 600 }}"     border-radius: 50px;

              alt="{{ block.settings.title }}"

              class="gb-category-image"  .gb-category-card:hover .gb-category-image {    color: #007AFF;

              loading="lazy">

          {% else %}    transform: scale(1.05);    font-size: 13px;

            {% comment %}Imagens Unsplash automáticas baseadas no título{% endcomment %}

            {% assign title_lower = block.settings.title | downcase %}  }    font-weight: 700;

            {% if title_lower contains 'protein' or title_lower contains 'proteína' or title_lower contains 'whey' %}

              {% comment %}Whey protein em pote - fundo clean{% endcomment %}    text-transform: uppercase;

              <img

                src="https://images.unsplash.com/photo-1593095948071-474c5cc2989d?w=600&h=800&fit=crop&q=80&auto=format"   .gb-category-overlay {    letter-spacing: 0.15em;

                alt="{{ block.settings.title }}"

                class="gb-category-image"    position: absolute;    margin-bottom: 24px;

                loading="lazy">

            {% elsif title_lower contains 'pré-treino' or title_lower contains 'pre' or title_lower contains 'energia' %}    inset: 0;  }

              {% comment %}Academia com equipamentos - alta energia{% endcomment %}

              <img     background: linear-gradient(180deg, transparent 0%, rgba(26, 26, 26, 0.8) 100%);

                src="https://images.unsplash.com/photo-1517836357463-d25dfeac3438?w=600&h=800&fit=crop&q=80&auto=format"

                alt="{{ block.settings.title }}"     display: flex;  .categories-heading {

                class="gb-category-image"

                loading="lazy">    align-items: flex-end;    font-size: clamp(42px, 6vw, 72px);

            {% elsif title_lower contains 'creatina' or title_lower contains 'creatine' or title_lower contains 'força' %}

              {% comment %}Atleta levantando peso - força{% endcomment %}    padding: 32px;    font-weight: 900;

              <img

                src="https://images.unsplash.com/photo-1571902943202-507ec2618e8f?w=600&h=800&fit=crop&q=80&auto=format"   }    color: white;

                alt="{{ block.settings.title }}"

                class="gb-category-image"    margin-bottom: 20px;

                loading="lazy">

            {% elsif title_lower contains 'vitamina' or title_lower contains 'vitamin' or title_lower contains 'saúde' %}  .gb-category-content h3 {    text-transform: uppercase;

              {% comment %}Frutas e vegetais saudáveis{% endcomment %}

              <img     font-size: 24px;    letter-spacing: -0.02em;

                src="https://images.unsplash.com/photo-1505751172876-fa1923c5c528?w=600&h=800&fit=crop&q=80&auto=format"

                alt="{{ block.settings.title }}"     font-weight: 700;    line-height: 1.1;

                class="gb-category-image"

                loading="lazy">    color: #ffffff;  }

            {% elsif title_lower contains 'bcaa' or title_lower contains 'amino' %}

              {% comment %}Shaker e suplementos - pós-treino{% endcomment %}    margin: 0 0 8px 0;

              <img

                src="https://images.unsplash.com/photo-1517836357463-d25dfeac3438?w=600&h=800&fit=crop&q=80&auto=format"   }  .categories-heading-highlight {

                alt="{{ block.settings.title }}"

                class="gb-category-image"    background: linear-gradient(135deg, #007AFF 0%, #30D158 100%);

                loading="lazy">

            {% elsif title_lower contains 'barra' or title_lower contains 'snack' %}  .gb-category-content p {    -webkit-background-clip: text;

              {% comment %}Barras de proteína{% endcomment %}

              <img     font-size: 14px;    -webkit-text-fill-color: transparent;

                src="https://images.unsplash.com/photo-1525385133512-2f3bdd039054?w=600&h=800&fit=crop&q=80&auto=format"

                alt="{{ block.settings.title }}"     color: rgba(255, 255, 255, 0.8);    background-clip: text;

                class="gb-category-image"

                loading="lazy">    margin: 0;  }

            {% else %}

              {% comment %}Imagem genérica - atleta treinando{% endcomment %}  }

              <img

                src="https://images.unsplash.com/photo-1534438327276-14e5300c3a48?w=600&h=800&fit=crop&q=80&auto=format"   .categories-description {

                alt="{{ block.settings.title }}"

                class="gb-category-image"  @media (max-width: 768px) {    font-size: 18px;

                loading="lazy">

            {% endif %}    .gb-categories {    font-weight: 500;

          {% endif %}

                padding: 60px 0;    color: rgba(255, 255, 255, 0.6);

          <div class="gb-category-overlay">

            <div class="gb-category-content">    }    max-width: 700px;

              <h3>{{ block.settings.title }}</h3>

              <p>{{ block.settings.description }}</p>    margin: 0 auto;

            </div>

          </div>    .gb-categories-container {    line-height: 1.7;

        </a>

      {% endfor %}      padding: 0 24px;  }

    </div>

  </div>    }

</section>

  /* ========================================

{% schema %}

{    .gb-categories-header {     🎯 CATEGORIES GRID

  "name": "Garcia Builder Categories",

  "tag": "section",      margin-bottom: 40px;  ======================================== */

  "class": "garcia-builder-categories",

  "settings": [    }  .categories-grid {

    {

      "type": "text",    display: grid;

      "id": "title",

      "label": "Título",    .gb-categories-grid {    grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));

      "default": "Categorias"

    },      grid-template-columns: 1fr;    gap: 30px;

    {

      "type": "text",      gap: 24px;    margin-bottom: 60px;

      "id": "subtitle",

      "label": "Subtítulo",    }  }

      "default": "Encontre os melhores suplementos para seu objetivo"

    }

  ],

  "blocks": [    .gb-category-card {  /* Category Card */

    {

      "type": "category",      height: 320px;  .category-card {

      "name": "Categoria",

      "settings": [    }    position: relative;

        {

          "type": "image_picker",  }    height: 500px;

          "id": "image",

          "label": "Imagem",</style>    border-radius: var(--category-radius);

          "info": "Recomendado: 600x800px. Deixe em branco para usar imagem automática do Unsplash baseada no título."

        },    overflow: hidden;

        {

          "type": "text",<section class="gb-categories">    cursor: pointer;

          "id": "title",

          "label": "Título",  <div class="gb-categories-container">    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);

          "default": "Proteínas",

          "info": "Palavras-chave: Proteína, Whey, Pré-Treino, Creatina, Vitaminas, BCAA geram imagens automáticas"    <div class="gb-categories-header">    animation: fade-slide-up 1s ease-out backwards;

        },

        {      <h2 class="gb-categories-title">{{ section.settings.title | default: 'Categorias' }}</h2>  }

          "type": "text",

          "id": "description",      <p class="gb-categories-subtitle">{{ section.settings.subtitle | default: 'Encontre os melhores suplementos para seu objetivo' }}</p>

          "label": "Descrição",

          "default": "Whey, Caseína e mais"    </div>  .category-card:nth-child(1) { animation-delay: 0.1s; }

        },

        {  .category-card:nth-child(2) { animation-delay: 0.2s; }

          "type": "url",

          "id": "link",    <div class="gb-categories-grid">  .category-card:nth-child(3) { animation-delay: 0.3s; }

          "label": "Link"

        }      {% for block in section.blocks %}  .category-card:nth-child(4) { animation-delay: 0.4s; }

      ]

    }        <a href="{{ block.settings.link }}" class="gb-category-card">  .category-card:nth-child(5) { animation-delay: 0.5s; }

  ],

  "presets": [          {% if block.settings.image != blank %}  .category-card:nth-child(6) { animation-delay: 0.6s; }

    {

      "name": "Garcia Builder Categories",            <img src="{{ block.settings.image | image_url: width: 600 }}" alt="{{ block.settings.title }}" class="gb-category-image">

      "blocks": [

        {          {% else %}  .category-card:hover {

          "type": "category",

          "settings": {            <img src="https://via.placeholder.com/600x800/1a1a1a/d4af37?text={{ block.settings.title }}" alt="{{ block.settings.title }}" class="gb-category-image">    transform: translateY(-15px) scale(1.02);

            "title": "Proteínas",

            "description": "Whey, Caseína, Isolado"          {% endif %}    box-shadow: 0 30px 80px rgba(0, 122, 255, 0.4);

          }

        },            }

        {

          "type": "category",          <div class="gb-category-overlay">

          "settings": {

            "title": "Pré-Treino",            <div class="gb-category-content">  /* Featured Card (First Item - Larger) */

            "description": "Energia e Foco"

          }              <h3>{{ block.settings.title }}</h3>  .category-card.featured {

        },

        {              <p>{{ block.settings.description }}</p>    grid-column: span 2;

          "type": "category",

          "settings": {            </div>    height: 600px;

            "title": "Creatina",

            "description": "Força e Performance"          </div>  }

          }

        },        </a>

        {

          "type": "category",      {% endfor %}  /* Background Image */

          "settings": {

            "title": "Vitaminas",    </div>  .category-image {

            "description": "Saúde e Bem-Estar"

          }  </div>    width: 100%;

        }

      ]</section>    height: 100%;

    }

  ]    object-fit: cover;

}

{% endschema %}{% schema %}    transition: transform 0.7s cubic-bezier(0.4, 0, 0.2, 1);


{  }

  "name": "Garcia Builder Categories",

  "tag": "section",  .category-card:hover .category-image {

  "class": "garcia-builder-categories",    transform: scale(1.1);

  "settings": [  }

    {

      "type": "text",  /* Gradient Overlay */

      "id": "title",  .category-overlay {

      "label": "Título",    position: absolute;

      "default": "Categorias"    inset: 0;

    },    background: var(--category-overlay);

    {    transition: opacity 0.5s ease;

      "type": "text",  }

      "id": "subtitle",

      "label": "Subtítulo",  .category-card:hover .category-overlay {

      "default": "Encontre os melhores suplementos para seu objetivo"    background: linear-gradient(180deg,

    }      transparent 0%,

  ],      rgba(0, 122, 255, 0.3) 50%,

  "blocks": [      rgba(0, 0, 0, 0.9) 100%

    {    );

      "type": "category",  }

      "name": "Categoria",

      "settings": [  /* Content */

        {  .category-content {

          "type": "image_picker",    position: absolute;

          "id": "image",    bottom: 0;

          "label": "Imagem"    left: 0;

        },    right: 0;

        {    padding: 40px;

          "type": "text",    z-index: 2;

          "id": "title",    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);

          "label": "Título",  }

          "default": "Proteínas"

        },  .category-card:hover .category-content {

        {    padding-bottom: 50px;

          "type": "text",  }

          "id": "description",

          "label": "Descrição",  /* Category Icon */

          "default": "Whey, Caseína e mais"  .category-icon {

        },    width: 70px;

        {    height: 70px;

          "type": "url",    background: linear-gradient(135deg, rgba(0, 122, 255, 0.3) 0%, rgba(48, 209, 88, 0.3) 100%);

          "id": "link",    backdrop-filter: blur(20px);

          "label": "Link"    border: 2px solid rgba(255, 255, 255, 0.2);

        }    border-radius: 18px;

      ]    display: flex;

    }    align-items: center;

  ],    justify-content: center;

  "presets": [    font-size: 32px;

    {    margin-bottom: 24px;

      "name": "Garcia Builder Categories",    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);

      "blocks": [    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);

        {  }

          "type": "category",

          "settings": {  .category-card:hover .category-icon {

            "title": "Proteínas",    transform: scale(1.1) rotate(5deg);

            "description": "Whey, Caseína, Isolado"    background: linear-gradient(135deg, rgba(0, 122, 255, 0.5) 0%, rgba(48, 209, 88, 0.5) 100%);

          }    box-shadow: 0 12px 40px rgba(0, 122, 255, 0.5);

        },  }

        {

          "type": "category",  /* Category Name */

          "settings": {  .category-name {

            "title": "Pré-Treino",    font-size: 32px;

            "description": "Energia e Foco"    font-weight: 900;

          }    color: white;

        },    margin-bottom: 12px;

        {    text-transform: uppercase;

          "type": "category",    letter-spacing: -0.02em;

          "settings": {    text-shadow: 0 4px 20px rgba(0, 0, 0, 0.8);

            "title": "Creatina",    transition: all 0.3s ease;

            "description": "Força e Performance"  }

          }

        }  .category-card:hover .category-name {

      ]    transform: translateX(10px);

    }    color: #007AFF;

  ]  }

}

{% endschema %}  /* Category Description */

  .category-description {
    font-size: 15px;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 20px;
    line-height: 1.6;
    max-height: 0;
    overflow: hidden;
    opacity: 0;
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .category-card:hover .category-description {
    max-height: 100px;
    opacity: 1;
    margin-bottom: 24px;
  }

  /* Product Count */
  .category-count {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 18px;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 10px;
    color: rgba(255, 255, 255, 0.8);
    font-size: 13px;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .category-card:hover .category-count {
    background: rgba(0, 122, 255, 0.3);
    border-color: rgba(0, 122, 255, 0.5);
    color: white;
  }

  /* CTA Arrow */
  .category-arrow {
    position: absolute;
    bottom: 40px;
    right: 40px;
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #007AFF 0%, #30D158 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    color: white;
    opacity: 0;
    transform: translateX(-20px);
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 8px 30px rgba(0, 122, 255, 0.5);
  }

  .category-card:hover .category-arrow {
    opacity: 1;
    transform: translateX(0);
  }

  /* ========================================
     🚀 VIEW ALL BUTTON
  ======================================== */
  .categories-footer {
    text-align: center;
    margin-top: 60px;
    animation: fade-slide-up 1s ease-out 0.8s backwards;
  }

  .view-all-button {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    padding: 20px 50px;
    background: linear-gradient(135deg, rgba(0, 122, 255, 0.2) 0%, rgba(48, 209, 88, 0.2) 100%);
    border: 2px solid rgba(0, 122, 255, 0.5);
    border-radius: 14px;
    color: white;
    font-size: 16px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    cursor: pointer;
    text-decoration: none;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .view-all-button::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, #007AFF 0%, #30D158 100%);
    opacity: 0;
    transition: opacity 0.4s ease;
  }

  .view-all-button:hover::before {
    opacity: 1;
  }

  .view-all-button:hover {
    transform: translateY(-4px) scale(1.05);
    border-color: transparent;
    box-shadow: 0 15px 50px rgba(0, 122, 255, 0.6);
  }

  .view-all-button span {
    position: relative;
    z-index: 1;
  }

  .view-all-arrow {
    font-size: 20px;
    transition: transform 0.4s ease;
    position: relative;
    z-index: 1;
  }

  .view-all-button:hover .view-all-arrow {
    transform: translateX(8px);
  }

  /* ========================================
     📱 RESPONSIVE DESIGN
  ======================================== */
  @media (max-width: 1200px) {
    .categories-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .category-card.featured {
      grid-column: span 2;
      height: 500px;
    }
  }

  @media (max-width: 768px) {
    .premium-master-categories {
      padding: 80px 20px;
    }

    .categories-header {
      margin-bottom: 50px;
    }

    .categories-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .category-card,
    .category-card.featured {
      height: 400px;
      grid-column: span 1;
    }

    .category-content {
      padding: 30px;
    }

    .category-icon {
      width: 55px;
      height: 55px;
      font-size: 26px;
    }

    .category-name {
      font-size: 26px;
    }

    .category-description {
      display: none;
    }

    .category-arrow {
      bottom: 30px;
      right: 30px;
      width: 50px;
      height: 50px;
      font-size: 20px;
    }

    .view-all-button {
      width: 100%;
      max-width: 100%;
      justify-content: center;
    }
  }

  /* ========================================
     ✨ ANIMATIONS
  ======================================== */
  @keyframes fade-slide-up {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

<!-- ========================================
     🏆 PREMIUM CATEGORIES HTML
======================================== -->

<section class="premium-master-categories">
  <div class="categories-bg-pattern"></div>

  <div class="categories-container">

    <!-- Section Header -->
    <div class="categories-header">
      {% if section.settings.badge_text != blank %}
        <span class="categories-badge">{{ section.settings.badge_text }}</span>
      {% endif %}

      <h2 class="categories-heading">
        {{ section.settings.heading | default: 'SHOP BY' }}
        <span class="categories-heading-highlight">{{ section.settings.heading_highlight | default: 'CATEGORY' }}</span>
      </h2>

      {% if section.settings.description != blank %}
        <p class="categories-description">{{ section.settings.description }}</p>
      {% endif %}
    </div>

    <!-- Categories Grid -->
    <div class="categories-grid">
      {% for block in section.blocks %}
        <a
          href="{{ block.settings.collection.url | default: '/collections/all' }}"
          class="category-card {% if forloop.first and section.settings.featured_first %}featured{% endif %}"
          {{ block.shopify_attributes }}
        >
          <!-- Background Image -->
          {% if block.settings.image != blank %}
            <img
              src="{{ block.settings.image | image_url: width: 800 }}"
              alt="{{ block.settings.title }}"
              width="800"
              height="600"
              loading="lazy"
              class="category-image"
            >
          {% else %}
            <div style="
              width: 100%;
              height: 100%;
              background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
            "></div>
          {% endif %}

          <!-- Overlay -->
          <div class="category-overlay"></div>

          <!-- Content -->
          <div class="category-content">
            {% if block.settings.icon != blank %}
              <div class="category-icon">{{ block.settings.icon }}</div>
            {% endif %}

            <h3 class="category-name">{{ block.settings.title | default: 'Category Name' }}</h3>

            {% if block.settings.description != blank %}
              <p class="category-description">{{ block.settings.description }}</p>
            {% endif %}

            {% if block.settings.collection != blank %}
              <span class="category-count">
                {{ block.settings.collection.products_count }} Products
              </span>
            {% endif %}
          </div>

          <!-- CTA Arrow -->
          <div class="category-arrow">→</div>
        </a>
      {% endfor %}
    </div>

    <!-- View All Button -->
    {% if section.settings.show_view_all %}
      <div class="categories-footer">
        <a href="{{ section.settings.view_all_link | default: '/collections' }}" class="view-all-button">
          <span>{{ section.settings.view_all_text | default: 'View All Categories' }}</span>
          <span class="view-all-arrow">→</span>
        </a>
      </div>
    {% endif %}

  </div>
</section>

{% schema %}
{
  "name": "Premium Master Categories",
  "tag": "section",
  "class": "premium-master-categories-section",
  "settings": [
    {
      "type": "header",
      "content": "Section Header"
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "Explore"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "SHOP BY"
    },
    {
      "type": "text",
      "id": "heading_highlight",
      "label": "Heading Highlight",
      "default": "CATEGORY"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Discover our premium collections designed for peak performance"
    },
    {
      "type": "header",
      "content": "Layout Options"
    },
    {
      "type": "checkbox",
      "id": "featured_first",
      "label": "Make first item featured (larger)",
      "default": true
    },
    {
      "type": "header",
      "content": "View All Button"
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "label": "Show View All Button",
      "default": true
    },
    {
      "type": "text",
      "id": "view_all_text",
      "label": "Button Text",
      "default": "View All Categories"
    },
    {
      "type": "url",
      "id": "view_all_link",
      "label": "Button Link"
    }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Category Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Category Image"
        },
        {
          "type": "text",
          "id": "icon",
          "label": "Icon (Emoji)",
          "default": "💪"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Category Title",
          "default": "Category Name"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Category Description",
          "default": "Premium products for your fitness journey"
        },
        {
          "type": "collection",
          "id": "collection",
          "label": "Link to Collection"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Premium Master Categories",
      "blocks": [
        {
          "type": "category",
          "settings": {
            "icon": "💪",
            "title": "Strength Training",
            "description": "Build muscle and increase power"
          }
        },
        {
          "type": "category",
          "settings": {
            "icon": "🏃",
            "title": "Cardio & Running",
            "description": "Boost endurance and stamina"
          }
        },
        {
          "type": "category",
          "settings": {
            "icon": "🧘",
            "title": "Yoga & Flexibility",
            "description": "Improve mobility and balance"
          }
        },
        {
          "type": "category",
          "settings": {
            "icon": "👕",
            "title": "Apparel",
            "description": "Premium athletic wear"
          }
        }
      ]
    }
  ]
}
{% endschema %}
