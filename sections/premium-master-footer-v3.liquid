<!--<!--

================================================================================

🏆 GARCIA BUILDER - FOOTER V3.0🏆 PREMIUM MASTER FOOTER V3.0

Clean & Professional FooterWorld-Class International Standards

========================================Inspired by: Nike, Gymshark, Lululemon, Adidas

-->Ultra-Premium Design | Trust Building

========================================

<style>-->

  .gb-footer {

    background: #1a1a1a;<link rel="stylesheet" href="{{ 'premium-v2.css' | asset_url }}">

    color: rgba(255, 255, 255, 0.8);

    padding: 60px 0 30px;<style>

  }  /* ========================================

     🎨 PREMIUM MASTER FOOTER VARIABLES

  .gb-footer-container {  ======================================== */

    max-width: 1400px;  :root {

    margin: 0 auto;    --footer-bg-primary: #000000;

    padding: 0 40px;    --footer-bg-secondary: #0a0a0a;

  }    --footer-text: rgba(255, 255, 255, 0.7);

    --footer-heading: #ffffff;

  .gb-footer-top {    --footer-accent: #007AFF;

    display: grid;  }

    grid-template-columns: 2fr 1fr 1fr 1fr;

    gap: 60px;  /* ========================================

    padding-bottom: 40px;     💎 MAIN FOOTER STRUCTURE

    border-bottom: 1px solid rgba(255, 255, 255, 0.1);  ======================================== */

    margin-bottom: 30px;  .premium-master-footer {

  }    background: linear-gradient(180deg, var(--footer-bg-primary) 0%, var(--footer-bg-secondary) 100%);

    color: var(--footer-text);

  .gb-footer-brand h3 {    position: relative;

    font-size: 24px;    overflow: hidden;

    font-weight: 900;  }

    color: #ffffff;

    margin: 0 0 16px 0;  /* Decorative Background Elements */

  }  .footer-bg-pattern {

    position: absolute;

  .gb-footer-brand p {    inset: 0;

    font-size: 14px;    opacity: 0.03;

    line-height: 1.7;    background-image:

    margin: 0 0 24px 0;      radial-gradient(circle at 20% 30%, rgba(0, 122, 255, 0.1) 0%, transparent 50%),

    max-width: 300px;      radial-gradient(circle at 80% 70%, rgba(48, 209, 88, 0.1) 0%, transparent 50%);

  }    pointer-events: none;

  }

  .gb-footer-social {

    display: flex;  .footer-container {

    gap: 16px;    max-width: 1600px;

  }    margin: 0 auto;

    padding: 0 40px;

  .gb-footer-social a {    position: relative;

    display: flex;    z-index: 1;

    align-items: center;  }

    justify-content: center;

    width: 40px;  /* ========================================

    height: 40px;     🚀 TOP SECTION - Newsletter & Social

    background: rgba(255, 255, 255, 0.1);  ======================================== */

    border-radius: 4px;  .footer-top {

    color: #ffffff;    padding: 80px 0 60px;

    text-decoration: none;    border-bottom: 1px solid rgba(255, 255, 255, 0.08);

    font-size: 18px;  }

    transition: all 0.3s ease;

  }  .footer-top-content {

    display: grid;

  .gb-footer-social a:hover {    grid-template-columns: 1.5fr 1fr;

    background: #d4af37;    gap: 60px;

    color: #1a1a1a;    align-items: center;

  }  }



  .gb-footer-column h4 {  .footer-newsletter {

    font-size: 14px;    max-width: 600px;

    font-weight: 700;  }

    color: #ffffff;

    text-transform: uppercase;  .newsletter-heading {

    letter-spacing: 1px;    font-size: 42px;

    margin: 0 0 20px 0;    font-weight: 900;

  }    color: var(--footer-heading);

    margin-bottom: 16px;

  .gb-footer-links {    text-transform: uppercase;

    list-style: none;    letter-spacing: -0.02em;

    padding: 0;    background: linear-gradient(135deg, #ffffff 0%, #007AFF 100%);

    margin: 0;    -webkit-background-clip: text;

  }    -webkit-text-fill-color: transparent;

    background-clip: text;

  .gb-footer-links li {  }

    margin-bottom: 12px;

  }  .newsletter-description {

    font-size: 16px;

  .gb-footer-links a {    line-height: 1.6;

    color: rgba(255, 255, 255, 0.7);    margin-bottom: 30px;

    text-decoration: none;    color: rgba(255, 255, 255, 0.6);

    font-size: 14px;  }

    transition: color 0.3s ease;

  }  .newsletter-form {

    display: flex;

  .gb-footer-links a:hover {    gap: 12px;

    color: #d4af37;    background: rgba(255, 255, 255, 0.05);

  }    padding: 8px;

    border-radius: 16px;

  .gb-footer-bottom {    border: 1px solid rgba(255, 255, 255, 0.1);

    display: flex;    transition: all 0.3s ease;

    justify-content: space-between;  }

    align-items: center;

    font-size: 13px;  .newsletter-form:focus-within {

  }    border-color: rgba(0, 122, 255, 0.5);

    box-shadow: 0 0 30px rgba(0, 122, 255, 0.2);

  .gb-footer-payment {  }

    display: flex;

    gap: 12px;  .newsletter-input {

    align-items: center;    flex: 1;

  }    padding: 18px 24px;

    background: transparent;

  .gb-footer-payment img {    border: none;

    height: 24px;    color: white;

    opacity: 0.6;    font-size: 15px;

  }    font-weight: 500;

    outline: none;

  @media (max-width: 1024px) {  }

    .gb-footer-top {

      grid-template-columns: repeat(2, 1fr);  .newsletter-input::placeholder {

      gap: 40px;    color: rgba(255, 255, 255, 0.4);

    }  }

  }

  .newsletter-button {

  @media (max-width: 640px) {    padding: 18px 40px;

    .gb-footer {    background: linear-gradient(135deg, #007AFF 0%, #0056CC 100%);

      padding: 40px 0 20px;    color: white;

    }    font-size: 15px;

    font-weight: 700;

    .gb-footer-container {    text-transform: uppercase;

      padding: 0 24px;    letter-spacing: 0.05em;

    }    border: none;

    border-radius: 12px;

    .gb-footer-top {    cursor: pointer;

      grid-template-columns: 1fr;    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);

      gap: 32px;    box-shadow: 0 4px 20px rgba(0, 122, 255, 0.3);

    }  }



    .gb-footer-bottom {  .newsletter-button:hover {

      flex-direction: column;    background: linear-gradient(135deg, #0056CC 0%, #003D99 100%);

      gap: 16px;    transform: translateY(-2px);

      text-align: center;    box-shadow: 0 8px 30px rgba(0, 122, 255, 0.5);

    }  }

  }

</style>  /* Social Media Links */

  .footer-social {

<footer class="gb-footer">    display: flex;

  <div class="gb-footer-container">    flex-direction: column;

    <div class="gb-footer-top">    align-items: flex-end;

      <!-- Brand Column -->  }

      <div class="gb-footer-brand">

        <h3>{{ section.settings.brand_name | default: 'Garcia Builder' }}</h3>  .social-heading {

        <p>{{ section.settings.brand_description | default: 'Suplementos premium de alta performance para resultados reais.' }}</p>    font-size: 18px;

        <div class="gb-footer-social">    font-weight: 700;

          {% if section.settings.instagram != blank %}    color: var(--footer-heading);

            <a href="{{ section.settings.instagram }}" target="_blank" aria-label="Instagram">📷</a>    margin-bottom: 24px;

          {% endif %}    text-transform: uppercase;

          {% if section.settings.facebook != blank %}    letter-spacing: 0.1em;

            <a href="{{ section.settings.facebook }}" target="_blank" aria-label="Facebook">👍</a>  }

          {% endif %}

          {% if section.settings.whatsapp != blank %}  .social-links {

            <a href="{{ section.settings.whatsapp }}" target="_blank" aria-label="WhatsApp">💬</a>    display: flex;

          {% endif %}    gap: 16px;

        </div>  }

      </div>

  .social-link {

      <!-- Links Columns -->    display: flex;

      {% for block in section.blocks %}    align-items: center;

        <div class="gb-footer-column">    justify-content: center;

          <h4>{{ block.settings.title }}</h4>    width: 55px;

          <ul class="gb-footer-links">    height: 55px;

            {% if block.settings.menu != blank %}    background: rgba(255, 255, 255, 0.05);

              {% for link in linklists[block.settings.menu].links %}    border: 1px solid rgba(255, 255, 255, 0.1);

                <li><a href="{{ link.url }}">{{ link.title }}</a></li>    border-radius: 14px;

              {% endfor %}    color: rgba(255, 255, 255, 0.8);

            {% endif %}    font-size: 24px;

          </ul>    text-decoration: none;

        </div>    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);

      {% endfor %}  }

    </div>

  .social-link:hover {

    <div class="gb-footer-bottom">    background: linear-gradient(135deg, rgba(0, 122, 255, 0.3) 0%, rgba(48, 209, 88, 0.3) 100%);

      <p>© {{ 'now' | date: '%Y' }} {{ shop.name }}. Todos os direitos reservados.</p>    border-color: rgba(0, 122, 255, 0.5);

      <div class="gb-footer-payment">    transform: translateY(-5px) scale(1.1);

        <span>Pagamento seguro</span>    box-shadow: 0 10px 30px rgba(0, 122, 255, 0.4);

      </div>  }

    </div>

  </div>  /* ========================================

</footer>     📋 MIDDLE SECTION - Navigation Links

  ======================================== */

{% schema %}  .footer-middle {

{    padding: 60px 0;

  "name": "Garcia Builder Footer",  }

  "tag": "footer",

  "class": "garcia-builder-footer",  .footer-grid {

  "settings": [    display: grid;

    {    grid-template-columns: 2fr repeat(4, 1fr);

      "type": "text",    gap: 50px;

      "id": "brand_name",  }

      "label": "Nome da Marca",

      "default": "Garcia Builder"  /* Brand Column */

    },  .footer-brand {

    {    padding-right: 40px;

      "type": "textarea",  }

      "id": "brand_description",

      "label": "Descrição",  .footer-logo {

      "default": "Suplementos premium de alta performance para resultados reais."    display: flex;

    },    align-items: center;

    {    gap: 12px;

      "type": "url",    margin-bottom: 24px;

      "id": "instagram",  }

      "label": "Instagram URL"

    },  .footer-logo-icon {

    {    width: 55px;

      "type": "url",    height: 55px;

      "id": "facebook",    background: linear-gradient(135deg, #007AFF 0%, #30D158 100%);

      "label": "Facebook URL"    border-radius: 14px;

    },    display: flex;

    {    align-items: center;

      "type": "url",    justify-content: center;

      "id": "whatsapp",    font-size: 28px;

      "label": "WhatsApp URL"    box-shadow: 0 4px 20px rgba(0, 122, 255, 0.3);

    }  }

  ],

  "blocks": [  .footer-logo-text {

    {    font-size: 28px;

      "type": "menu",    font-weight: 900;

      "name": "Coluna de Links",    color: white;

      "settings": [    text-transform: uppercase;

        {    letter-spacing: -0.02em;

          "type": "text",  }

          "id": "title",

          "label": "Título",  .footer-brand-description {

          "default": "Links"    font-size: 15px;

        },    line-height: 1.7;

        {    color: rgba(255, 255, 255, 0.5);

          "type": "link_list",    margin-bottom: 30px;

          "id": "menu",  }

          "label": "Menu"

        }  .footer-trust-badges {

      ]    display: flex;

    }    gap: 12px;

  ],    flex-wrap: wrap;

  "presets": [  }

    {

      "name": "Garcia Builder Footer",  .trust-badge {

      "blocks": [    display: flex;

        {    align-items: center;

          "type": "menu",    gap: 8px;

          "settings": {    padding: 10px 16px;

            "title": "Loja"    background: rgba(255, 255, 255, 0.05);

          }    border: 1px solid rgba(255, 255, 255, 0.1);

        },    border-radius: 10px;

        {    font-size: 13px;

          "type": "menu",    font-weight: 600;

          "settings": {    color: rgba(255, 255, 255, 0.7);

            "title": "Ajuda"  }

          }

        }  .trust-badge-icon {

      ]    font-size: 18px;

    }  }

  ]

}  /* Navigation Columns */

{% endschema %}  .footer-column h4 {

    font-size: 16px;
    font-weight: 700;
    color: var(--footer-heading);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 24px;
  }

  .footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .footer-links li {
    margin-bottom: 14px;
  }

  .footer-links a {
    color: rgba(255, 255, 255, 0.6);
    font-size: 15px;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.2s ease;
    display: inline-block;
  }

  .footer-links a:hover {
    color: #007AFF;
    transform: translateX(5px);
  }

  /* ========================================
     🎯 BOTTOM SECTION - Legal & Payment
  ======================================== */
  .footer-bottom {
    padding: 30px 0;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }

  .footer-bottom-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 30px;
  }

  .footer-legal {
    display: flex;
    align-items: center;
    gap: 30px;
  }

  .footer-copyright {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.4);
  }

  .footer-legal-links {
    display: flex;
    gap: 20px;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .footer-legal-links a {
    color: rgba(255, 255, 255, 0.5);
    font-size: 13px;
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .footer-legal-links a:hover {
    color: #007AFF;
  }

  /* Payment Methods */
  .footer-payment {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .payment-icon {
    width: 50px;
    height: 32px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    transition: all 0.2s ease;
  }

  .payment-icon:hover {
    background: rgba(255, 255, 255, 0.1);
    transform: scale(1.05);
  }

  /* ========================================
     📱 RESPONSIVE DESIGN
  ======================================== */
  @media (max-width: 1200px) {
    .footer-grid {
      grid-template-columns: 1fr 1fr;
      gap: 40px;
    }

    .footer-brand {
      grid-column: 1 / -1;
    }
  }

  @media (max-width: 768px) {
    .footer-container {
      padding: 0 20px;
    }

    .footer-top {
      padding: 60px 0 40px;
    }

    .footer-top-content {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .footer-social {
      align-items: flex-start;
    }

    .newsletter-heading {
      font-size: 32px;
    }

    .newsletter-form {
      flex-direction: column;
    }

    .footer-grid {
      grid-template-columns: 1fr;
      gap: 30px;
    }

    .footer-bottom-content {
      flex-direction: column;
      text-align: center;
    }

    .footer-legal {
      flex-direction: column;
      gap: 20px;
    }

    .footer-payment {
      justify-content: center;
    }
  }
</style>

<!-- ========================================
     🏆 PREMIUM MASTER FOOTER HTML
======================================== -->

<footer class="premium-master-footer">
  <div class="footer-bg-pattern"></div>

  <div class="footer-container">

    <!-- Top Section: Newsletter & Social -->
    <div class="footer-top">
      <div class="footer-top-content">
        <div class="footer-newsletter">
          <h2 class="newsletter-heading">{{ section.settings.newsletter_heading | default: 'Join The Movement' }}</h2>
          <p class="newsletter-description">
            {{ section.settings.newsletter_description | default: 'Get exclusive access to new products, workouts, and special offers. Join 100,000+ fitness enthusiasts worldwide.' }}
          </p>
          {% form 'customer', class: 'newsletter-form' %}
            <input
              type="email"
              name="contact[email]"
              placeholder="Enter your email"
              class="newsletter-input"
              required
            >
            <button type="submit" class="newsletter-button">
              Subscribe
            </button>
          {% endform %}
        </div>

        <div class="footer-social">
          <h3 class="social-heading">Follow Us</h3>
          <div class="social-links">
            {% if section.settings.instagram_url != blank %}
              <a href="{{ section.settings.instagram_url }}" class="social-link" target="_blank" aria-label="Instagram">📷</a>
            {% endif %}
            {% if section.settings.facebook_url != blank %}
              <a href="{{ section.settings.facebook_url }}" class="social-link" target="_blank" aria-label="Facebook">📘</a>
            {% endif %}
            {% if section.settings.tiktok_url != blank %}
              <a href="{{ section.settings.tiktok_url }}" class="social-link" target="_blank" aria-label="TikTok">🎵</a>
            {% endif %}
            {% if section.settings.youtube_url != blank %}
              <a href="{{ section.settings.youtube_url }}" class="social-link" target="_blank" aria-label="YouTube">🎥</a>
            {% endif %}
          </div>
        </div>
      </div>
    </div>

    <!-- Middle Section: Navigation Links -->
    <div class="footer-middle">
      <div class="footer-grid">

        <!-- Brand Column -->
        <div class="footer-brand">
          <div class="footer-logo">
            <div class="footer-logo-icon">💪</div>
            <span class="footer-logo-text">{{ shop.name }}</span>
          </div>
          <p class="footer-brand-description">
            {{ section.settings.brand_description | default: 'Premium fitness gear for athletes who demand excellence. Built for performance, designed for champions.' }}
          </p>
          <div class="footer-trust-badges">
            <div class="trust-badge">
              <span class="trust-badge-icon">✓</span>
              <span>Secure Checkout</span>
            </div>
            <div class="trust-badge">
              <span class="trust-badge-icon">🚚</span>
              <span>Free Shipping</span>
            </div>
            <div class="trust-badge">
              <span class="trust-badge-icon">↩️</span>
              <span>Easy Returns</span>
            </div>
          </div>
        </div>

        <!-- Dynamic Columns from Blocks -->
        {% for block in section.blocks %}
          {% case block.type %}
            {% when 'column' %}
              <div class="footer-column">
                <h4>{{ block.settings.title }}</h4>
                <ul class="footer-links">
                  {% if block.settings.menu != blank %}
                    {% for link in linklists[block.settings.menu].links %}
                      <li><a href="{{ link.url }}">{{ link.title }}</a></li>
                    {% endfor %}
                  {% endif %}
                </ul>
              </div>
          {% endcase %}
        {% endfor %}

      </div>
    </div>

    <!-- Bottom Section: Legal & Payment -->
    <div class="footer-bottom">
      <div class="footer-bottom-content">
        <div class="footer-legal">
          <p class="footer-copyright">
            {{ section.settings.copyright | default: '© 2025 All rights reserved' }}
          </p>
          <ul class="footer-legal-links">
            <li><a href="/pages/privacy-policy">Privacy Policy</a></li>
            <li><a href="/pages/terms-of-service">Terms of Service</a></li>
            <li><a href="/pages/shipping">Shipping Policy</a></li>
          </ul>
        </div>

        <div class="footer-payment">
          <div class="payment-icon">💳</div>
          <div class="payment-icon">🅿️</div>
          <div class="payment-icon">🔒</div>
        </div>
      </div>
    </div>

  </div>
</footer>

{% schema %}
{
  "name": "Premium Master Footer",
  "tag": "footer",
  "class": "premium-master-footer-section",
  "settings": [
    {
      "type": "header",
      "content": "Newsletter Section"
    },
    {
      "type": "text",
      "id": "newsletter_heading",
      "label": "Newsletter Heading",
      "default": "Join The Movement"
    },
    {
      "type": "textarea",
      "id": "newsletter_description",
      "label": "Newsletter Description",
      "default": "Get exclusive access to new products, workouts, and special offers."
    },
    {
      "type": "header",
      "content": "Social Media Links"
    },
    {
      "type": "url",
      "id": "instagram_url",
      "label": "Instagram URL"
    },
    {
      "type": "url",
      "id": "facebook_url",
      "label": "Facebook URL"
    },
    {
      "type": "url",
      "id": "tiktok_url",
      "label": "TikTok URL"
    },
    {
      "type": "url",
      "id": "youtube_url",
      "label": "YouTube URL"
    },
    {
      "type": "header",
      "content": "Brand Section"
    },
    {
      "type": "textarea",
      "id": "brand_description",
      "label": "Brand Description",
      "default": "Premium fitness gear for athletes who demand excellence."
    },
    {
      "type": "text",
      "id": "copyright",
      "label": "Copyright Text",
      "default": "© 2025 All rights reserved"
    }
  ],
  "blocks": [
    {
      "type": "column",
      "name": "Footer Column",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Column Title"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Menu"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Premium Master Footer",
      "blocks": [
        {
          "type": "column",
          "settings": {
            "title": "Shop"
          }
        },
        {
          "type": "column",
          "settings": {
            "title": "About"
          }
        },
        {
          "type": "column",
          "settings": {
            "title": "Support"
          }
        },
        {
          "type": "column",
          "settings": {
            "title": "Resources"
          }
        }
      ]
    }
  ]
}
{% endschema %}
