<!--<!--<!--

========================================

🏆 GARCIA BUILDER - CLEAN HERO V3.0================================================================================

Premium Supplement Store | Minimalist Design

========================================🏆 GARCIA BUILDER - CLEAN HERO V3.0🏆 PREMIUM MASTER HERO V3.0

-->

Premium Supplement Store | Minimalist DesignWorld-Class Hero Section

<style>

  .gb-hero {========================================Inspired by: Nike, Gymshark, Lululemon

    position: relative;

    height: 85vh;-->Ultra-Premium Backgrounds | Maximum Impact

    min-height: 600px;

    display: flex;========================================

    align-items: center;

    overflow: hidden;<style>-->

    background: #1a1a1a;

  }  .gb-hero {



  .gb-hero-bg {    position: relative;<link rel="stylesheet" href="{{ 'premium-v2.css' | asset_url }}">

    position: absolute;

    inset: 0;    height: 85vh;

    z-index: 1;

  }    min-height: 600px;<style>



  .gb-hero-bg img {    display: flex;  /* ========================================

    width: 100%;

    height: 100%;    align-items: center;     🎨 PREMIUM MASTER HERO VARIABLES

    object-fit: cover;

    opacity: 0.5;    overflow: hidden;  ======================================== */

  }

    background: #1a1a1a;  :root {

  .gb-hero-overlay {

    position: absolute;  }    --hero-height: 90vh;

    inset: 0;

    background: linear-gradient(90deg, rgba(26, 26, 26, 0.95) 0%, rgba(26, 26, 26, 0.3) 100%);    --hero-overlay: linear-gradient(135deg, rgba(0, 0, 0, 0.7) 0%, rgba(0, 0, 0, 0.3) 100%);

    z-index: 2;

  }  .gb-hero-bg {  }



  .gb-hero-content {    position: absolute;

    position: relative;

    z-index: 3;    inset: 0;  /* ========================================

    max-width: 1400px;

    margin: 0 auto;    z-index: 1;     💎 HERO SECTION - Full Screen Impact

    padding: 0 40px;

    width: 100%;  }  ======================================== */

  }

  .premium-master-hero {

  .gb-hero-badge {

    display: inline-block;  .gb-hero-bg img {    position: relative;

    padding: 8px 16px;

    background: rgba(212, 175, 55, 0.1);    width: 100%;    height: var(--hero-height);

    border: 1px solid rgba(212, 175, 55, 0.3);

    border-radius: 20px;    height: 100%;    min-height: 600px;

    color: #d4af37;

    font-size: 12px;    object-fit: cover;    display: flex;

    font-weight: 600;

    text-transform: uppercase;    opacity: 0.6;    align-items: center;

    letter-spacing: 1.5px;

    margin-bottom: 24px;  }    justify-content: center;

  }

    overflow: hidden;

  .gb-hero-title {

    font-size: clamp(48px, 7vw, 80px);  .gb-hero-overlay {  }

    font-weight: 900;

    line-height: 1.1;    position: absolute;

    color: #ffffff;

    margin: 0 0 24px 0;    inset: 0;  /* Background Image/Video Layer */

    max-width: 800px;

    letter-spacing: -0.02em;    background: linear-gradient(90deg, rgba(26, 26, 26, 0.95) 0%, rgba(26, 26, 26, 0.4) 100%);  .hero-background {

  }

    z-index: 2;    position: absolute;

  .gb-hero-title span {

    color: #d4af37;  }    inset: 0;

  }

    z-index: 1;

  .gb-hero-subtitle {

    font-size: clamp(18px, 2vw, 22px);  .gb-hero-content {  }

    font-weight: 400;

    line-height: 1.6;    position: relative;

    color: rgba(255, 255, 255, 0.8);

    margin: 0 0 40px 0;    z-index: 3;  .hero-bg-image {

    max-width: 600px;

  }    max-width: 1400px;    width: 100%;



  .gb-hero-actions {    margin: 0 auto;    height: 100%;

    display: flex;

    gap: 16px;    padding: 0 40px;    object-fit: cover;

    flex-wrap: wrap;

  }    width: 100%;    animation: ken-burns 30s ease-in-out infinite alternate;



  .gb-hero-btn {  }  }

    padding: 16px 40px;

    font-size: 15px;

    font-weight: 600;

    text-transform: uppercase;  .gb-hero-badge {  @keyframes ken-burns {

    letter-spacing: 1px;

    text-decoration: none;    display: inline-block;    0% { transform: scale(1); }

    border-radius: 4px;

    transition: all 0.3s ease;    padding: 8px 16px;    100% { transform: scale(1.1); }

    display: inline-block;

  }    background: rgba(212, 175, 55, 0.1);  }



  .gb-hero-btn-primary {    border: 1px solid rgba(212, 175, 55, 0.3);

    background: #d4af37;

    color: #1a1a1a;    border-radius: 20px;  .hero-bg-video {

  }

    color: #d4af37;    width: 100%;

  .gb-hero-btn-primary:hover {

    background: #ffffff;    font-size: 12px;    height: 100%;

    transform: translateY(-2px);

    box-shadow: 0 8px 20px rgba(212, 175, 55, 0.3);    font-weight: 600;    object-fit: cover;

  }

    text-transform: uppercase;  }

  .gb-hero-btn-secondary {

    background: transparent;    letter-spacing: 1.5px;

    color: #ffffff;

    border: 2px solid rgba(255, 255, 255, 0.3);    margin-bottom: 24px;  /* Gradient Overlay */

  }

  }  .hero-overlay {

  .gb-hero-btn-secondary:hover {

    border-color: #ffffff;    position: absolute;

    background: rgba(255, 255, 255, 0.1);

  }  .gb-hero-title {    inset: 0;



  @media (max-width: 768px) {    font-size: clamp(48px, 7vw, 80px);    background: var(--hero-overlay);

    .gb-hero {

      height: 70vh;    font-weight: 900;    z-index: 2;

      min-height: 500px;

    }    line-height: 1.1;  }



    .gb-hero-content {    color: #ffffff;

      padding: 0 24px;

    }    margin: 0 0 24px 0;  /* Animated Gradient Overlay (Optional) */



    .gb-hero-title {    max-width: 800px;  .hero-gradient-animated {

      margin-bottom: 16px;

    }    letter-spacing: -0.02em;    position: absolute;



    .gb-hero-subtitle {  }    inset: 0;

      margin-bottom: 32px;

    }    background: linear-gradient(



    .gb-hero-actions {  .gb-hero-title span {      135deg,

      flex-direction: column;

    }    color: #d4af37;      rgba(0, 122, 255, 0.3) 0%,



    .gb-hero-btn {  }      rgba(48, 209, 88, 0.2) 50%,

      width: 100%;

      text-align: center;      rgba(0, 122, 255, 0.3) 100%

    }

  }  .gb-hero-subtitle {    );

</style>

    font-size: clamp(18px, 2vw, 22px);    background-size: 200% 200%;

<section class="gb-hero">

  <div class="gb-hero-bg">    font-weight: 400;    animation: gradient-flow 8s ease infinite;

    {% if section.settings.hero_image != blank %}

      <img     line-height: 1.6;    z-index: 3;

        src="{{ section.settings.hero_image | image_url: width: 1920 }}" 

        alt="{{ section.settings.title }}"    color: rgba(255, 255, 255, 0.8);    mix-blend-mode: overlay;

        loading="eager">

    {% else %}    margin: 0 0 40px 0;  }

      <!-- Unsplash: Atleta masculino treinando com dumbbells em academia moderna -->

      <img     max-width: 600px;

        src="https://images.unsplash.com/photo-1534438327276-14e5300c3a48?w=1920&h=1080&fit=crop&q=80&auto=format" 

        alt="Garcia Builder - Suplementos Premium"  }  @keyframes gradient-flow {

        loading="eager">

    {% endif %}    0% { background-position: 0% 50%; }

  </div>

    .gb-hero-actions {    50% { background-position: 100% 50%; }

  <div class="gb-hero-overlay"></div>

      display: flex;    100% { background-position: 0% 50%; }

  <div class="gb-hero-content">

    {% if section.settings.badge != blank %}    gap: 16px;  }

      <span class="gb-hero-badge">{{ section.settings.badge }}</span>

    {% endif %}    flex-wrap: wrap;

    

    <h1 class="gb-hero-title">  }  /* Particles Effect */

      {{ section.settings.title | default: 'Construa o Corpo dos Seus <span>Sonhos</span>' }}

    </h1>  .hero-particles {

    

    <p class="gb-hero-subtitle">  .gb-hero-btn {    position: absolute;

      {{ section.settings.subtitle | default: 'Suplementos premium de alta performance para resultados reais. Qualidade internacional, entrega nacional.' }}

    </p>    padding: 16px 40px;    inset: 0;

    

    <div class="gb-hero-actions">    font-size: 15px;    z-index: 4;

      {% if section.settings.primary_button_text != blank %}

        <a href="{{ section.settings.primary_button_link | default: '#' }}" class="gb-hero-btn gb-hero-btn-primary">    font-weight: 600;    opacity: 0.2;

          {{ section.settings.primary_button_text }}

        </a>    text-transform: uppercase;  }

      {% endif %}

          letter-spacing: 1px;

      {% if section.settings.secondary_button_text != blank %}

        <a href="{{ section.settings.secondary_button_link | default: '#' }}" class="gb-hero-btn gb-hero-btn-secondary">    text-decoration: none;  .particle {

          {{ section.settings.secondary_button_text }}

        </a>    border-radius: 4px;    position: absolute;

      {% endif %}

    </div>    transition: all 0.3s ease;    width: 4px;

  </div>

</section>    display: inline-block;    height: 4px;



{% schema %}  }    background: white;

{

  "name": "Garcia Builder Hero",    border-radius: 50%;

  "tag": "section",

  "class": "garcia-builder-hero",  .gb-hero-btn-primary {    animation: float-particle linear infinite;

  "settings": [

    {    background: #d4af37;  }

      "type": "image_picker",

      "id": "hero_image",    color: #1a1a1a;

      "label": "Imagem de Fundo",

      "info": "Recomendado: 1920x1080px. Deixe em branco para usar imagem padrão do Unsplash."  }  @keyframes float-particle {

    },

    {    0% {

      "type": "text",

      "id": "badge",  .gb-hero-btn-primary:hover {      transform: translateY(0) translateX(0);

      "label": "Badge (opcional)",

      "default": "✨ Novo Lançamento"    background: #ffffff;      opacity: 0;

    },

    {    transform: translateY(-2px);    }

      "type": "html",

      "id": "title",    box-shadow: 0 8px 20px rgba(212, 175, 55, 0.3);    10% {

      "label": "Título",

      "default": "Construa o Corpo dos Seus <span>Sonhos</span>",  }      opacity: 1;

      "info": "Use <span> para destacar palavras em dourado"

    },    }

    {

      "type": "textarea",  .gb-hero-btn-secondary {    90% {

      "id": "subtitle",

      "label": "Subtítulo",    background: transparent;      opacity: 1;

      "default": "Suplementos premium de alta performance para resultados reais. Qualidade internacional, entrega nacional."

    },    color: #ffffff;    }

    {

      "type": "text",    border: 2px solid rgba(255, 255, 255, 0.3);    100% {

      "id": "primary_button_text",

      "label": "Botão Principal - Texto",  }      transform: translateY(-100vh) translateX(50px);

      "default": "Ver Produtos"

    },      opacity: 0;

    {

      "type": "url",  .gb-hero-btn-secondary:hover {    }

      "id": "primary_button_link",

      "label": "Botão Principal - Link"    border-color: #ffffff;  }

    },

    {    background: rgba(255, 255, 255, 0.1);

      "type": "text",

      "id": "secondary_button_text",  }  /* Content Container */

      "label": "Botão Secundário - Texto",

      "default": "Saiba Mais"  .hero-content {

    },

    {  @media (max-width: 768px) {    position: relative;

      "type": "url",

      "id": "secondary_button_link",    .gb-hero {    z-index: 10;

      "label": "Botão Secundário - Link"

    }      height: 70vh;    max-width: 1200px;

  ],

  "presets": [      min-height: 500px;    padding: 0 40px;

    {

      "name": "Garcia Builder Hero"    }    text-align: center;

    }

  ]    animation: fade-slide-up 1.2s ease-out;

}

{% endschema %}    .gb-hero-content {  }


      padding: 0 24px;

    }  @keyframes fade-slide-up {

    from {

    .gb-hero-title {      opacity: 0;

      margin-bottom: 16px;      transform: translateY(40px);

    }    }

    to {

    .gb-hero-subtitle {      opacity: 1;

      margin-bottom: 32px;      transform: translateY(0);

    }    }

  }

    .gb-hero-actions {

      flex-direction: column;  /* Pre-heading Badge */

    }  .hero-badge {

    display: inline-flex;

    .gb-hero-btn {    align-items: center;

      width: 100%;    gap: 10px;

      text-align: center;    padding: 12px 28px;

    }    background: rgba(255, 255, 255, 0.15);

  }    backdrop-filter: blur(20px);

</style>    border: 1px solid rgba(255, 255, 255, 0.2);

    border-radius: 50px;

<section class="gb-hero">    color: white;

  <div class="gb-hero-bg">    font-size: 14px;

    {% if section.settings.hero_image != blank %}    font-weight: 700;

      <img src="{{ section.settings.hero_image | image_url: width: 1920 }}" alt="{{ section.settings.title }}">    text-transform: uppercase;

    {% else %}    letter-spacing: 0.1em;

      <img src="https://images.unsplash.com/photo-1534438327276-14e5300c3a48?w=1920&q=80" alt="Garcia Builder">    margin-bottom: 30px;

    {% endif %}    animation: fade-slide-up 1.2s ease-out 0.2s both;

  </div>  }



  <div class="gb-hero-overlay"></div>  .hero-badge-icon {

      font-size: 18px;

  <div class="gb-hero-content">    animation: pulse-scale 2s ease-in-out infinite;

    {% if section.settings.badge != blank %}  }

      <span class="gb-hero-badge">{{ section.settings.badge }}</span>

    {% endif %}  /* Main Heading */

      .hero-heading {

    <h1 class="gb-hero-title">    font-size: clamp(48px, 8vw, 96px);

      {{ section.settings.title | default: 'Construa o Corpo dos Seus <span>Sonhos</span>' }}    font-weight: 900;

    </h1>    color: white;

        line-height: 1.1;

    <p class="gb-hero-subtitle">    margin-bottom: 24px;

      {{ section.settings.subtitle | default: 'Suplementos premium de alta performance para resultados reais. Qualidade internacional, entrega nacional.' }}    text-transform: uppercase;

    </p>    letter-spacing: -0.03em;

        text-shadow: 0 4px 30px rgba(0, 0, 0, 0.5);

    <div class="gb-hero-actions">    animation: fade-slide-up 1.2s ease-out 0.4s both;

      {% if section.settings.primary_button_text != blank %}  }

        <a href="{{ section.settings.primary_button_link | default: '#' }}" class="gb-hero-btn gb-hero-btn-primary">

          {{ section.settings.primary_button_text }}  .hero-heading-highlight {

        </a>    background: linear-gradient(135deg, #007AFF 0%, #30D158 100%);

      {% endif %}    -webkit-background-clip: text;

          -webkit-text-fill-color: transparent;

      {% if section.settings.secondary_button_text != blank %}    background-clip: text;

        <a href="{{ section.settings.secondary_button_link | default: '#' }}" class="gb-hero-btn gb-hero-btn-secondary">    display: inline-block;

          {{ section.settings.secondary_button_text }}  }

        </a>

      {% endif %}  /* Subheading */

    </div>  .hero-subheading {

  </div>    font-size: clamp(18px, 2.5vw, 24px);

</section>    font-weight: 500;

    color: rgba(255, 255, 255, 0.9);

{% schema %}    line-height: 1.6;

{    max-width: 700px;

  "name": "Garcia Builder Hero",    margin: 0 auto 40px;

  "tag": "section",    text-shadow: 0 2px 15px rgba(0, 0, 0, 0.5);

  "class": "garcia-builder-hero",    animation: fade-slide-up 1.2s ease-out 0.6s both;

  "settings": [  }

    {

      "type": "image_picker",  /* CTA Buttons */

      "id": "hero_image",  .hero-cta-group {

      "label": "Imagem de Fundo"    display: flex;

    },    gap: 20px;

    {    justify-content: center;

      "type": "text",    flex-wrap: wrap;

      "id": "badge",    animation: fade-slide-up 1.2s ease-out 0.8s both;

      "label": "Badge (opcional)",  }

      "default": "✨ Novo"

    },  .hero-cta-primary {

    {    padding: 20px 50px;

      "type": "text",    background: linear-gradient(135deg, #007AFF 0%, #0056CC 100%);

      "id": "title",    color: white;

      "label": "Título",    font-size: 18px;

      "default": "Construa o Corpo dos Seus <span>Sonhos</span>"    font-weight: 700;

    },    text-transform: uppercase;

    {    letter-spacing: 0.05em;

      "type": "textarea",    border: none;

      "id": "subtitle",    border-radius: 14px;

      "label": "Subtítulo",    cursor: pointer;

      "default": "Suplementos premium de alta performance para resultados reais. Qualidade internacional, entrega nacional."    text-decoration: none;

    },    display: inline-block;

    {    box-shadow: 0 10px 40px rgba(0, 122, 255, 0.5);

      "type": "text",    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);

      "id": "primary_button_text",    position: relative;

      "label": "Botão Principal - Texto",    overflow: hidden;

      "default": "Ver Produtos"  }

    },

    {  .hero-cta-primary::before {

      "type": "url",    content: '';

      "id": "primary_button_link",    position: absolute;

      "label": "Botão Principal - Link"    inset: 0;

    },    background: linear-gradient(135deg, #0056CC 0%, #003D99 100%);

    {    opacity: 0;

      "type": "text",    transition: opacity 0.4s ease;

      "id": "secondary_button_text",  }

      "label": "Botão Secundário - Texto",

      "default": "Saiba Mais"  .hero-cta-primary:hover::before {

    },    opacity: 1;

    {  }

      "type": "url",

      "id": "secondary_button_link",  .hero-cta-primary:hover {

      "label": "Botão Secundário - Link"    transform: translateY(-4px) scale(1.05);

    }    box-shadow: 0 15px 50px rgba(0, 122, 255, 0.7);

  ],  }

  "presets": [

    {  .hero-cta-primary span {

      "name": "Garcia Builder Hero"    position: relative;

    }    z-index: 1;

  ]  }

}

{% endschema %}  .hero-cta-secondary {

    padding: 20px 50px;
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(20px);
    color: white;
    font-size: 18px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-radius: 14px;
    cursor: pointer;
    text-decoration: none;
    display: inline-block;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .hero-cta-secondary:hover {
    background: rgba(255, 255, 255, 0.25);
    border-color: rgba(255, 255, 255, 0.5);
    transform: translateY(-4px) scale(1.05);
    box-shadow: 0 10px 40px rgba(255, 255, 255, 0.2);
  }

  /* Stats/Social Proof */
  .hero-stats {
    display: flex;
    justify-content: center;
    gap: 60px;
    margin-top: 60px;
    animation: fade-slide-up 1.2s ease-out 1s both;
  }

  .hero-stat-item {
    text-align: center;
  }

  .hero-stat-number {
    font-size: 42px;
    font-weight: 900;
    color: white;
    display: block;
    margin-bottom: 8px;
    text-shadow: 0 2px 15px rgba(0, 0, 0, 0.5);
  }

  .hero-stat-label {
    font-size: 14px;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.7);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* Scroll Indicator */
  .hero-scroll-indicator {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 10;
    animation: fade-in 1.2s ease-out 1.2s both;
  }

  .scroll-arrow {
    width: 30px;
    height: 50px;
    border: 2px solid rgba(255, 255, 255, 0.5);
    border-radius: 25px;
    position: relative;
  }

  .scroll-arrow::before {
    content: '';
    position: absolute;
    top: 10px;
    left: 50%;
    transform: translateX(-50%);
    width: 6px;
    height: 6px;
    background: white;
    border-radius: 50%;
    animation: scroll-bounce 2s ease-in-out infinite;
  }

  @keyframes scroll-bounce {
    0%, 100% { transform: translateX(-50%) translateY(0); opacity: 1; }
    50% { transform: translateX(-50%) translateY(15px); opacity: 0.5; }
  }

  @keyframes fade-in {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  /* ========================================
     📱 RESPONSIVE DESIGN
  ======================================== */
  @media (max-width: 768px) {
    .premium-master-hero {
      height: 80vh;
      min-height: 500px;
    }

    .hero-content {
      padding: 0 20px;
    }

    .hero-badge {
      font-size: 12px;
      padding: 10px 20px;
    }

    .hero-cta-group {
      flex-direction: column;
      gap: 16px;
    }

    .hero-cta-primary,
    .hero-cta-secondary {
      padding: 18px 40px;
      font-size: 16px;
      width: 100%;
      max-width: 300px;
    }

    .hero-stats {
      flex-direction: column;
      gap: 30px;
    }

    .hero-scroll-indicator {
      display: none;
    }
  }
</style>

<!-- ========================================
     🏆 PREMIUM MASTER HERO HTML
======================================== -->

<section class="premium-master-hero">

  <!-- Background Layer -->
  <div class="hero-background">
    {% if section.settings.background_video_url != blank %}
      <video
        class="hero-bg-video"
        autoplay
        muted
        loop
        playsinline
        poster="{{ section.settings.background_image | image_url: width: 1920 }}"
      >
        <source src="{{ section.settings.background_video_url }}" type="video/mp4">
      </video>
    {% elsif section.settings.background_image != blank %}
      <img
        src="{{ section.settings.background_image | image_url: width: 1920 }}"
        alt="{{ section.settings.heading }}"
        width="1920"
        height="1080"
        class="hero-bg-image"
        loading="eager"
      >
    {% else %}
      <div style="
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg,
          #000000 0%,
          #1a1a1a 50%,
          #000000 100%
        );
      "></div>
    {% endif %}
  </div>

  <!-- Overlay -->
  <div class="hero-overlay"></div>

  {% if section.settings.enable_animated_gradient %}
    <div class="hero-gradient-animated"></div>
  {% endif %}

  <!-- Particles Effect -->
  {% if section.settings.enable_particles %}
    <div class="hero-particles">
      {% for i in (1..20) %}
        <div class="particle" style="
          left: {{ 5 | times: i }}%;
          animation-duration: {{ 10 | plus: i }}s;
          animation-delay: {{ i | times: 0.5 }}s;
        "></div>
      {% endfor %}
    </div>
  {% endif %}

  <!-- Content -->
  <div class="hero-content">

    {% if section.settings.badge_text != blank %}
      <div class="hero-badge">
        <span class="hero-badge-icon">{{ section.settings.badge_icon | default: '🔥' }}</span>
        <span>{{ section.settings.badge_text }}</span>
      </div>
    {% endif %}

    <h1 class="hero-heading">
      {{ section.settings.heading | default: 'TRANSFORM YOUR' }}
      <span class="hero-heading-highlight">{{ section.settings.heading_highlight | default: 'FITNESS JOURNEY' }}</span>
    </h1>

    {% if section.settings.subheading != blank %}
      <p class="hero-subheading">
        {{ section.settings.subheading }}
      </p>
    {% endif %}

    <div class="hero-cta-group">
      {% if section.settings.primary_button_text != blank %}
        <a href="{{ section.settings.primary_button_link }}" class="hero-cta-primary">
          <span>{{ section.settings.primary_button_text }}</span>
        </a>
      {% endif %}

      {% if section.settings.secondary_button_text != blank %}
        <a href="{{ section.settings.secondary_button_link | default: '#' }}" class="hero-cta-secondary">
          {{ section.settings.secondary_button_text }}
        </a>
      {% endif %}
    </div>

    {% if section.settings.show_stats %}
      <div class="hero-stats">
        <div class="hero-stat-item">
          <span class="hero-stat-number">{{ section.settings.stat_1_number | default: '100K+' }}</span>
          <span class="hero-stat-label">{{ section.settings.stat_1_label | default: 'Happy Customers' }}</span>
        </div>
        <div class="hero-stat-item">
          <span class="hero-stat-number">{{ section.settings.stat_2_number | default: '4.9/5' }}</span>
          <span class="hero-stat-label">{{ section.settings.stat_2_label | default: 'Average Rating' }}</span>
        </div>
        <div class="hero-stat-item">
          <span class="hero-stat-number">{{ section.settings.stat_3_number | default: '50+' }}</span>
          <span class="hero-stat-label">{{ section.settings.stat_3_label | default: 'Countries' }}</span>
        </div>
      </div>
    {% endif %}

  </div>

  <!-- Scroll Indicator -->
  <div class="hero-scroll-indicator">
    <div class="scroll-arrow"></div>
  </div>

</section>

{% schema %}
{
  "name": "Premium Master Hero",
  "tag": "section",
  "class": "premium-master-hero-section",
  "settings": [
    {
      "type": "header",
      "content": "Background Settings"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background Image"
    },
    {
      "type": "url",
      "id": "background_video_url",
      "label": "Background Video URL (MP4)",
      "info": "Video will override image"
    },
    {
      "type": "checkbox",
      "id": "enable_animated_gradient",
      "label": "Enable Animated Gradient Overlay",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_particles",
      "label": "Enable Particles Effect",
      "default": true
    },
    {
      "type": "header",
      "content": "Badge"
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "New Collection 2025"
    },
    {
      "type": "text",
      "id": "badge_icon",
      "label": "Badge Icon (Emoji)",
      "default": "🔥"
    },
    {
      "type": "header",
      "content": "Heading"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Main Heading",
      "default": "TRANSFORM YOUR"
    },
    {
      "type": "text",
      "id": "heading_highlight",
      "label": "Highlighted Text",
      "default": "FITNESS JOURNEY"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Premium athletic wear engineered for peak performance"
    },
    {
      "type": "header",
      "content": "Buttons"
    },
    {
      "type": "text",
      "id": "primary_button_text",
      "label": "Primary Button Text",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "primary_button_link",
      "label": "Primary Button Link"
    },
    {
      "type": "text",
      "id": "secondary_button_text",
      "label": "Secondary Button Text",
      "default": "Learn More"
    },
    {
      "type": "url",
      "id": "secondary_button_link",
      "label": "Secondary Button Link"
    },
    {
      "type": "header",
      "content": "Stats Section"
    },
    {
      "type": "checkbox",
      "id": "show_stats",
      "label": "Show Stats",
      "default": true
    },
    {
      "type": "text",
      "id": "stat_1_number",
      "label": "Stat 1 Number",
      "default": "100K+"
    },
    {
      "type": "text",
      "id": "stat_1_label",
      "label": "Stat 1 Label",
      "default": "Happy Customers"
    },
    {
      "type": "text",
      "id": "stat_2_number",
      "label": "Stat 2 Number",
      "default": "4.9/5"
    },
    {
      "type": "text",
      "id": "stat_2_label",
      "label": "Stat 2 Label",
      "default": "Average Rating"
    },
    {
      "type": "text",
      "id": "stat_3_number",
      "label": "Stat 3 Number",
      "default": "50+"
    },
    {
      "type": "text",
      "id": "stat_3_label",
      "label": "Stat 3 Label",
      "default": "Countries"
    }
  ],
  "presets": [
    {
      "name": "Premium Master Hero"
    }
  ]
}
{% endschema %}
