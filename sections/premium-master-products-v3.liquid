<!--<!--<!--

========================================

🏆 GARCIA BUILDER - PRODUCTS V3.0================================================================================

Clean Product Showcase

========================================🏆 GARCIA BUILDER - PRODUCTS V3.0🏆 PREMIUM MASTER PRODUCTS V3.0

-->

Clean Product ShowcaseWorld-Class Product Showcase

<style>

  .gb-products {========================================Inspired by: Nike, Gymshark, Lululemon

    padding: 100px 0;

    background: #f5f5f5;-->Ultra-Premium Grid | Interactive Hover Effects

  }

========================================

  .gb-products-container {

    max-width: 1400px;<style>-->

    margin: 0 auto;

    padding: 0 40px;  .gb-products {

  }

    padding: 100px 0;<link rel="stylesheet" href="{{ 'premium-v2.css' | asset_url }}">

  .gb-products-header {

    text-align: center;    background: #f5f5f5;

    margin-bottom: 60px;

  }  }<style>



  .gb-products-title {  /* ========================================

    font-size: clamp(36px, 5vw, 48px);

    font-weight: 900;  .gb-products-container {     🎨 PREMIUM PRODUCTS VARIABLES

    color: #1a1a1a;

    margin: 0 0 16px 0;    max-width: 1400px;  ======================================== */

    letter-spacing: -0.02em;

  }    margin: 0 auto;  :root {



  .gb-products-subtitle {    padding: 0 40px;    --products-spacing: 120px;

    font-size: 18px;

    color: #6b6b6b;  }    --product-card-radius: 20px;

    margin: 0 0 32px 0;

  }  }



  .gb-products-cta {  .gb-products-header {

    display: inline-block;

    padding: 12px 32px;    text-align: center;  /* ========================================

    background: #1a1a1a;

    color: #ffffff;    margin-bottom: 60px;     💎 MAIN SECTION

    font-size: 14px;

    font-weight: 600;  }  ======================================== */

    text-transform: uppercase;

    letter-spacing: 1px;  .premium-master-products {

    text-decoration: none;

    border-radius: 4px;  .gb-products-title {    padding: var(--products-spacing) 40px;

    transition: all 0.3s ease;

  }    font-size: clamp(36px, 5vw, 48px);    background: #000000;



  .gb-products-cta:hover {    font-weight: 900;    position: relative;

    background: #d4af37;

    color: #1a1a1a;    color: #1a1a1a;  }

  }

    margin: 0 0 16px 0;

  .gb-products-grid {

    display: grid;    letter-spacing: -0.02em;  .products-container {

    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));

    gap: 32px;  }    max-width: 1600px;

  }

    margin: 0 auto;

  .gb-product-card {

    background: #ffffff;  .gb-products-subtitle {  }

    border-radius: 8px;

    overflow: hidden;    font-size: 18px;

    text-decoration: none;

    transition: transform 0.3s ease, box-shadow 0.3s ease;    color: #6b6b6b;  /* ========================================

    display: block;

  }    margin: 0 0 32px 0;     📝 SECTION HEADER



  .gb-product-card:hover {  }  ======================================== */

    transform: translateY(-4px);

    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.1);  .products-header {

  }

  .gb-products-cta {    text-align: center;

  .gb-product-image-wrapper {

    position: relative;    display: inline-block;    margin-bottom: 80px;

    padding-top: 100%;

    overflow: hidden;    padding: 12px 32px;    animation: fade-slide-up 0.8s ease-out;

    background: #f8f8f8;

  }    background: #1a1a1a;  }



  .gb-product-image {    color: #ffffff;

    position: absolute;

    top: 0;    font-size: 14px;  .products-badge {

    left: 0;

    width: 100%;    font-weight: 600;    display: inline-block;

    height: 100%;

    object-fit: contain;    text-transform: uppercase;    padding: 10px 24px;

    padding: 20px;

  }    letter-spacing: 1px;    background: linear-gradient(135deg, rgba(0, 122, 255, 0.2) 0%, rgba(48, 209, 88, 0.2) 100%);



  .gb-product-badge {    text-decoration: none;    border: 1px solid rgba(0, 122, 255, 0.3);

    position: absolute;

    top: 16px;    border-radius: 4px;    border-radius: 50px;

    left: 16px;

    padding: 6px 12px;    transition: all 0.3s ease;    color: #007AFF;

    background: #d4af37;

    color: #1a1a1a;  }    font-size: 13px;

    font-size: 11px;

    font-weight: 700;    font-weight: 700;

    text-transform: uppercase;

    letter-spacing: 0.5px;  .gb-products-cta:hover {    text-transform: uppercase;

    border-radius: 4px;

  }    background: #d4af37;    letter-spacing: 0.15em;



  .gb-product-info {    color: #1a1a1a;    margin-bottom: 24px;

    padding: 24px;

  }  }  }



  .gb-product-title {

    font-size: 18px;

    font-weight: 700;  .gb-products-grid {  .products-heading {

    color: #1a1a1a;

    margin: 0 0 8px 0;    display: grid;    font-size: clamp(42px, 6vw, 72px);

    display: -webkit-box;

    -webkit-line-clamp: 2;    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));    font-weight: 900;

    -webkit-box-orient: vertical;

    overflow: hidden;    gap: 32px;    color: white;

  }

  }    margin-bottom: 20px;

  .gb-product-price {

    font-size: 24px;    text-transform: uppercase;

    font-weight: 900;

    color: #d4af37;  .gb-product-card {    letter-spacing: -0.02em;

    margin: 0;

  }    background: #ffffff;    line-height: 1.1;



  .gb-product-price-compare {    border-radius: 8px;  }

    font-size: 16px;

    color: #999;    overflow: hidden;

    text-decoration: line-through;

    margin-left: 8px;    text-decoration: none;  .products-heading-highlight {

  }

    transition: transform 0.3s ease, box-shadow 0.3s ease;    background: linear-gradient(135deg, #007AFF 0%, #30D158 100%);

  @media (max-width: 768px) {

    .gb-products {  }    -webkit-background-clip: text;

      padding: 60px 0;

    }    -webkit-text-fill-color: transparent;



    .gb-products-container {  .gb-product-card:hover {    background-clip: text;

      padding: 0 24px;

    }    transform: translateY(-4px);  }



    .gb-products-header {    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.1);

      margin-bottom: 40px;

    }  }  .products-description {



    .gb-products-grid {    font-size: 18px;

      grid-template-columns: repeat(2, 1fr);

      gap: 16px;  .gb-product-image-wrapper {    font-weight: 500;

    }

    position: relative;    color: rgba(255, 255, 255, 0.6);

    .gb-product-info {

      padding: 16px;    padding-top: 100%;    max-width: 700px;

    }

  }    overflow: hidden;    margin: 0 auto;

</style>

    background: #f8f8f8;    line-height: 1.7;

<section class="gb-products">

  <div class="gb-products-container">  }  }

    <div class="gb-products-header">

      <h2 class="gb-products-title">{{ section.settings.title | default: 'Produtos em Destaque' }}</h2>

      <p class="gb-products-subtitle">{{ section.settings.subtitle | default: 'Os melhores suplementos do mercado' }}</p>

      {% if section.settings.button_text != blank %}  .gb-product-image {  /* ========================================

        <a href="{{ section.settings.button_link }}" class="gb-products-cta">{{ section.settings.button_text }}</a>

      {% endif %}    position: absolute;     🎯 PRODUCTS GRID

    </div>

    top: 0;  ======================================== */

    <div class="gb-products-grid">

      {% if section.settings.collection != blank %}    left: 0;  .products-grid {

        {% for product in collections[section.settings.collection].products limit: section.settings.products_count %}

          <a href="{{ product.url }}" class="gb-product-card">    width: 100%;    display: grid;

            <div class="gb-product-image-wrapper">

              {% if product.featured_image != blank %}    height: 100%;    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));

                <img 

                  src="{{ product.featured_image | image_url: width: 600 }}"     object-fit: contain;    gap: 30px;

                  alt="{{ product.title }}" 

                  class="gb-product-image"    padding: 20px;    margin-bottom: 60px;

                  loading="lazy">

              {% else %}  }  }

                {% comment %}Imagem padrão - suplemento genérico{% endcomment %}

                <img 

                  src="https://images.unsplash.com/photo-1593095948071-474c5cc2989d?w=600&h=600&fit=crop&q=80&auto=format" 

                  alt="{{ product.title }}"   .gb-product-badge {  /* Product Card */

                  class="gb-product-image"

                  loading="lazy">    position: absolute;  .product-card {

              {% endif %}

                  top: 16px;    background: rgba(255, 255, 255, 0.03);

              {% if product.compare_at_price > product.price %}

                <span class="gb-product-badge">Oferta</span>    left: 16px;    border: 1px solid rgba(255, 255, 255, 0.08);

              {% endif %}

            </div>    padding: 6px 12px;    border-radius: var(--product-card-radius);

            

            <div class="gb-product-info">    background: #d4af37;    overflow: hidden;

              <h3 class="gb-product-title">{{ product.title }}</h3>

              <p class="gb-product-price">    color: #1a1a1a;    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);

                {{ product.price | money }}

                {% if product.compare_at_price > product.price %}    font-size: 11px;    position: relative;

                  <span class="gb-product-price-compare">{{ product.compare_at_price | money }}</span>

                {% endif %}    font-weight: 700;    animation: fade-slide-up 0.8s ease-out backwards;

              </p>

            </div>    text-transform: uppercase;  }

          </a>

        {% endfor %}    letter-spacing: 0.5px;

      {% else %}

        {% comment %}Produtos de exemplo caso não tenha coleção{% endcomment %}    border-radius: 4px;  .product-card:nth-child(1) { animation-delay: 0.1s; }

        <div class="gb-product-card" style="pointer-events: none;">

          <div class="gb-product-image-wrapper">  }  .product-card:nth-child(2) { animation-delay: 0.2s; }

            <img 

              src="https://images.unsplash.com/photo-1593095948071-474c5cc2989d?w=600&h=600&fit=crop&q=80&auto=format"   .product-card:nth-child(3) { animation-delay: 0.3s; }

              alt="Whey Protein" 

              class="gb-product-image">  .gb-product-info {  .product-card:nth-child(4) { animation-delay: 0.4s; }

            <span class="gb-product-badge">Oferta</span>

          </div>    padding: 24px;  .product-card:nth-child(5) { animation-delay: 0.5s; }

          <div class="gb-product-info">

            <h3 class="gb-product-title">Whey Protein Isolado 1kg</h3>  }  .product-card:nth-child(6) { animation-delay: 0.6s; }

            <p class="gb-product-price">R$ 189,90</p>

          </div>

        </div>

      {% endif %}  .gb-product-title {  .product-card:hover {

    </div>

  </div>    font-size: 18px;    transform: translateY(-10px);

</section>

    font-weight: 700;    border-color: rgba(0, 122, 255, 0.3);

{% schema %}

{    color: #1a1a1a;    box-shadow: 0 20px 60px rgba(0, 122, 255, 0.3);

  "name": "Garcia Builder Products",

  "tag": "section",    margin: 0 0 8px 0;    background: rgba(255, 255, 255, 0.05);

  "class": "garcia-builder-products",

  "settings": [    display: -webkit-box;  }

    {

      "type": "text",    -webkit-line-clamp: 2;

      "id": "title",

      "label": "Título",    -webkit-box-orient: vertical;  /* Product Image Container */

      "default": "Produtos em Destaque"

    },    overflow: hidden;  .product-image-wrapper {

    {

      "type": "text",  }    position: relative;

      "id": "subtitle",

      "label": "Subtítulo",    aspect-ratio: 1 / 1;

      "default": "Os melhores suplementos do mercado"

    },  .gb-product-price {    overflow: hidden;

    {

      "type": "collection",    font-size: 24px;    background: rgba(255, 255, 255, 0.02);

      "id": "collection",

      "label": "Coleção"    font-weight: 900;  }

    },

    {    color: #d4af37;

      "type": "range",

      "id": "products_count",    margin: 0;  .product-image {

      "label": "Número de Produtos",

      "min": 3,  }    width: 100%;

      "max": 12,

      "step": 1,    height: 100%;

      "default": 8

    },  .gb-product-price-compare {    object-fit: cover;

    {

      "type": "text",    font-size: 16px;    transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);

      "id": "button_text",

      "label": "Texto Botão",    color: #999;  }

      "default": "Ver Todos"

    },    text-decoration: line-through;

    {

      "type": "url",    margin-left: 8px;  .product-card:hover .product-image {

      "id": "button_link",

      "label": "Link Botão"  }    transform: scale(1.08);

    }

  ],  }

  "presets": [

    {  @media (max-width: 768px) {

      "name": "Garcia Builder Products"

    }    .gb-products {  /* Product Badges */

  ]

}      padding: 60px 0;  .product-badges {

{% endschema %}

    }    position: absolute;

    top: 16px;

    .gb-products-container {    left: 16px;

      padding: 0 24px;    display: flex;

    }    flex-direction: column;

    gap: 8px;

    .gb-products-header {    z-index: 2;

      margin-bottom: 40px;  }

    }

  .product-badge {

    .gb-products-grid {    padding: 8px 16px;

      grid-template-columns: repeat(2, 1fr);    background: rgba(0, 0, 0, 0.8);

      gap: 16px;    backdrop-filter: blur(10px);

    }    border-radius: 8px;

    font-size: 12px;

    .gb-product-info {    font-weight: 700;

      padding: 16px;    text-transform: uppercase;

    }    letter-spacing: 0.05em;

  }  }

</style>

  .badge-new {

<section class="gb-products">    color: #30D158;

  <div class="gb-products-container">    border: 1px solid rgba(48, 209, 88, 0.3);

    <div class="gb-products-header">  }

      <h2 class="gb-products-title">{{ section.settings.title | default: 'Produtos em Destaque' }}</h2>

      <p class="gb-products-subtitle">{{ section.settings.subtitle | default: 'Os melhores suplementos do mercado' }}</p>  .badge-sale {

      {% if section.settings.button_text != blank %}    color: #FF3B30;

        <a href="{{ section.settings.button_link }}" class="gb-products-cta">{{ section.settings.button_text }}</a>    border: 1px solid rgba(255, 59, 48, 0.3);

      {% endif %}  }

    </div>

  .badge-bestseller {

    <div class="gb-products-grid">    color: #FF9500;

      {% if section.settings.collection != blank %}    border: 1px solid rgba(255, 149, 0, 0.3);

        {% for product in collections[section.settings.collection].products limit: section.settings.products_count %}  }

          <a href="{{ product.url }}" class="gb-product-card">

            <div class="gb-product-image-wrapper">  /* Quick Actions */

              {% if product.featured_image != blank %}  .product-actions {

                <img src="{{ product.featured_image | image_url: width: 600 }}" alt="{{ product.title }}" class="gb-product-image">    position: absolute;

              {% else %}    top: 16px;

                <img src="https://via.placeholder.com/600/f5f5f5/1a1a1a?text={{ product.title }}" alt="{{ product.title }}" class="gb-product-image">    right: 16px;

              {% endif %}    display: flex;

                  flex-direction: column;

              {% if product.compare_at_price > product.price %}    gap: 10px;

                <span class="gb-product-badge">Oferta</span>    opacity: 0;

              {% endif %}    transform: translateX(20px);

            </div>    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);

              }

            <div class="gb-product-info">

              <h3 class="gb-product-title">{{ product.title }}</h3>  .product-card:hover .product-actions {

              <p class="gb-product-price">    opacity: 1;

                {{ product.price | money }}    transform: translateX(0);

                {% if product.compare_at_price > product.price %}  }

                  <span class="gb-product-price-compare">{{ product.compare_at_price | money }}</span>

                {% endif %}  .action-btn {

              </p>    width: 45px;

            </div>    height: 45px;

          </a>    background: rgba(255, 255, 255, 0.95);

        {% endfor %}    backdrop-filter: blur(10px);

      {% endif %}    border: none;

    </div>    border-radius: 12px;

  </div>    display: flex;

</section>    align-items: center;

    justify-content: center;

{% schema %}    font-size: 18px;

{    cursor: pointer;

  "name": "Garcia Builder Products",    transition: all 0.3s ease;

  "tag": "section",    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);

  "class": "garcia-builder-products",  }

  "settings": [

    {  .action-btn:hover {

      "type": "text",    transform: scale(1.1);

      "id": "title",    background: #007AFF;

      "label": "Título",    color: white;

      "default": "Produtos em Destaque"  }

    },

    {  /* Quick Add Button */

      "type": "text",  .quick-add-btn {

      "id": "subtitle",    position: absolute;

      "label": "Subtítulo",    bottom: 16px;

      "default": "Os melhores suplementos do mercado"    left: 16px;

    },    right: 16px;

    {    padding: 16px;

      "type": "collection",    background: linear-gradient(135deg, #007AFF 0%, #0056CC 100%);

      "id": "collection",    color: white;

      "label": "Coleção"    font-size: 15px;

    },    font-weight: 700;

    {    text-transform: uppercase;

      "type": "range",    letter-spacing: 0.05em;

      "id": "products_count",    border: none;

      "label": "Número de Produtos",    border-radius: 12px;

      "min": 3,    cursor: pointer;

      "max": 12,    opacity: 0;

      "step": 1,    transform: translateY(10px);

      "default": 8    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);

    },    box-shadow: 0 8px 30px rgba(0, 122, 255, 0.5);

    {  }

      "type": "text",

      "id": "button_text",  .product-card:hover .quick-add-btn {

      "label": "Texto Botão",    opacity: 1;

      "default": "Ver Todos"    transform: translateY(0);

    },  }

    {

      "type": "url",  .quick-add-btn:hover {

      "id": "button_link",    background: linear-gradient(135deg, #0056CC 0%, #003D99 100%);

      "label": "Link Botão"    transform: translateY(-2px);

    }  }

  ],

  "presets": [  /* Product Info */

    {  .product-info {

      "name": "Garcia Builder Products"    padding: 24px;

    }  }

  ]

}  /* Product Vendor */

{% endschema %}  .product-vendor {

    font-size: 12px;
    font-weight: 600;
    color: #007AFF;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 8px;
  }

  /* Product Title */
  .product-title {
    font-size: 18px;
    font-weight: 700;
    color: white;
    margin-bottom: 12px;
    line-height: 1.4;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    transition: color 0.3s ease;
  }

  .product-card:hover .product-title {
    color: #007AFF;
  }

  /* Product Rating */
  .product-rating {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 14px;
  }

  .rating-stars {
    color: #FF9500;
    font-size: 14px;
    letter-spacing: 2px;
  }

  .rating-count {
    font-size: 13px;
    color: rgba(255, 255, 255, 0.5);
  }

  /* Product Price */
  .product-price {
    display: flex;
    align-items: baseline;
    gap: 12px;
  }

  .price-current {
    font-size: 24px;
    font-weight: 900;
    color: white;
  }

  .price-original {
    font-size: 18px;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.4);
    text-decoration: line-through;
  }

  .price-discount {
    padding: 4px 10px;
    background: linear-gradient(135deg, #FF3B30 0%, #FF9500 100%);
    color: white;
    font-size: 12px;
    font-weight: 700;
    border-radius: 6px;
  }

  /* ========================================
     🚀 VIEW ALL BUTTON
  ======================================== */
  .products-footer {
    text-align: center;
    animation: fade-slide-up 0.8s ease-out 0.8s backwards;
  }

  .view-all-button {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    padding: 20px 50px;
    background: linear-gradient(135deg, rgba(0, 122, 255, 0.2) 0%, rgba(48, 209, 88, 0.2) 100%);
    border: 2px solid rgba(0, 122, 255, 0.5);
    border-radius: 14px;
    color: white;
    font-size: 16px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    cursor: pointer;
    text-decoration: none;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .view-all-button::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, #007AFF 0%, #30D158 100%);
    opacity: 0;
    transition: opacity 0.4s ease;
  }

  .view-all-button:hover::before {
    opacity: 1;
  }

  .view-all-button:hover {
    transform: translateY(-4px) scale(1.05);
    border-color: transparent;
    box-shadow: 0 15px 50px rgba(0, 122, 255, 0.6);
  }

  .view-all-button span {
    position: relative;
    z-index: 1;
  }

  /* ========================================
     📱 RESPONSIVE DESIGN
  ======================================== */
  @media (max-width: 1200px) {
    .products-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 992px) {
    .products-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .premium-master-products {
      padding: 80px 20px;
    }

    .products-header {
      margin-bottom: 50px;
    }

    .products-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .product-actions {
      opacity: 1;
      transform: translateX(0);
    }

    .quick-add-btn {
      opacity: 1;
      transform: translateY(0);
    }

    .view-all-button {
      width: 100%;
      justify-content: center;
    }
  }

  /* ========================================
     ✨ ANIMATIONS
  ======================================== */
  @keyframes fade-slide-up {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

<!-- ========================================
     🏆 PREMIUM PRODUCTS HTML
======================================== -->

<section class="premium-master-products">
  <div class="products-container">

    <!-- Section Header -->
    <div class="products-header">
      {% if section.settings.badge_text != blank %}
        <span class="products-badge">{{ section.settings.badge_text }}</span>
      {% endif %}

      <h2 class="products-heading">
        {{ section.settings.heading | default: 'FEATURED' }}
        <span class="products-heading-highlight">{{ section.settings.heading_highlight | default: 'PRODUCTS' }}</span>
      </h2>

      {% if section.settings.description != blank %}
        <p class="products-description">{{ section.settings.description }}</p>
      {% endif %}
    </div>

    <!-- Products Grid -->
    <div class="products-grid">
      {% assign collection = collections[section.settings.collection] %}
      {% assign product_limit = section.settings.products_to_show | default: 6 %}

      {% for product in collection.products limit: product_limit %}
        <div class="product-card">
          <!-- Product Image -->
          <div class="product-image-wrapper">
            {% if product.featured_image %}
              <img
                src="{{ product.featured_image | image_url: width: 600 }}"
                alt="{{ product.title }}"
                width="600"
                height="600"
                loading="lazy"
                class="product-image"
              >
            {% else %}
              <div style="
                width: 100%;
                height: 100%;
                background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
              "></div>
            {% endif %}

            <!-- Badges -->
            <div class="product-badges">
              {% if product.tags contains 'new' %}
                <span class="product-badge badge-new">New</span>
              {% endif %}
              {% if product.compare_at_price > product.price %}
                <span class="product-badge badge-sale">Sale</span>
              {% endif %}
              {% if product.tags contains 'bestseller' %}
                <span class="product-badge badge-bestseller">Bestseller</span>
              {% endif %}
            </div>

            <!-- Quick Actions -->
            <div class="product-actions">
              <button class="action-btn" aria-label="Add to wishlist">❤️</button>
              <button class="action-btn" aria-label="Quick view">👁️</button>
            </div>

            <!-- Quick Add Button -->
            <button class="quick-add-btn" data-product-id="{{ product.id }}">
              Quick Add
            </button>
          </div>

          <!-- Product Info -->
          <a href="{{ product.url }}" class="product-info">
            {% if product.vendor != blank %}
              <div class="product-vendor">{{ product.vendor }}</div>
            {% endif %}

            <h3 class="product-title">{{ product.title }}</h3>

            {% if section.settings.show_rating %}
              <div class="product-rating">
                <span class="rating-stars">★★★★★</span>
                <span class="rating-count">(4.8)</span>
              </div>
            {% endif %}

            <div class="product-price">
              <span class="price-current">{{ product.price | money }}</span>
              {% if product.compare_at_price > product.price %}
                <span class="price-original">{{ product.compare_at_price | money }}</span>
                {% assign discount = product.compare_at_price | minus: product.price | times: 100 | divided_by: product.compare_at_price %}
                <span class="price-discount">-{{ discount }}%</span>
              {% endif %}
            </div>
          </a>
        </div>
      {% endfor %}
    </div>

    <!-- View All Button -->
    {% if section.settings.show_view_all %}
      <div class="products-footer">
        <a href="{{ collection.url | default: '/collections/all' }}" class="view-all-button">
          <span>{{ section.settings.view_all_text | default: 'View All Products' }}</span>
          <span>→</span>
        </a>
      </div>
    {% endif %}

  </div>
</section>

{% schema %}
{
  "name": "Premium Master Products",
  "tag": "section",
  "class": "premium-master-products-section",
  "settings": [
    {
      "type": "header",
      "content": "Section Header"
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "Best Sellers"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "FEATURED"
    },
    {
      "type": "text",
      "id": "heading_highlight",
      "label": "Heading Highlight",
      "default": "PRODUCTS"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Premium gear trusted by athletes worldwide"
    },
    {
      "type": "header",
      "content": "Products Settings"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "label": "Products to Show",
      "min": 2,
      "max": 12,
      "step": 1,
      "default": 6
    },
    {
      "type": "checkbox",
      "id": "show_rating",
      "label": "Show Product Rating",
      "default": true
    },
    {
      "type": "header",
      "content": "View All Button"
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "label": "Show View All Button",
      "default": true
    },
    {
      "type": "text",
      "id": "view_all_text",
      "label": "Button Text",
      "default": "View All Products"
    }
  ],
  "presets": [
    {
      "name": "Premium Master Products"
    }
  ]
}
{% endschema %}
